# 02. 이론 — 서버가 죽은 것을 사람 없이 어떻게 아는가

## 목차

- 사전 지식. 5주차 구조에서 무엇만 달라지는가
- 0. 실습 공통: 이번 주차의 판정 근거
- 1. 앱이 자기 상태를 숫자로 내놓기
- 2. Prometheus가 그 숫자를 모으기
- 3. PromQL — 쌓인 숫자에서 답을 꺼내기
- 4. Grafana가 보여 주기
- 5. 사람이 안 보고 있어도 알기 — 경고
- 6. 같은 조건으로 비교하기 — k6
- 7. 죽은 앱을 자동으로 살리기 — Restart Policy
- 8. 누가 복구하고 누가 관측하는가
- 참고. 이 구조가 아직 해결하지 못한 것
- 참고 자료

---

## 사전 지식. 5주차 구조에서 무엇만 달라지는가

5주차에서 `app1`을 중지했을 때 서비스가 유지되는 것을 확인했다. 확인 방법은 이랬다.

```bash
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8080/owners?study=app1-down"
docker compose logs --since 5m nginx | grep 'study=app1-down'
grep -Rc "study=app1-down" logs/tomcat/app2
```

이 방법에는 세 가지 한계가 있다.

| 한계 | 구체적으로 | 6주차에서 해결하는 도구 |
| --- | --- | --- |
| 사람이 직접 봐야 한다 | 명령을 입력한 순간의 상태만 알 수 있다 | Prometheus + Grafana 경고 |
| 되는지 안 되는지만 안다 | "느려졌다"는 알 수 없다 | Micrometer가 만드는 응답시간 지표 |
| 조건이 매번 다르다 | 지난주 결과와 비교할 수 없다 | k6의 일정한 요청 |

그리고 5주차에는 복구도 사람이 했다. `docker compose start app1`을 직접 입력했다. 이번 주차에는 Docker가 대신하게 한다.

| 5주차 | 6주차 |
| --- | --- |
| 로그를 사람이 읽어서 판단 | 앱이 숫자를 내놓고 도구가 수집 |
| 명령을 친 순간의 상태만 확인 | 시간 축에 쌓아 두고 나중에 비교 |
| "200이 나온다" | "p95가 180ms에서 340ms로 올랐다" |
| 사람이 `start`로 복구 | Docker가 자동으로 다시 실행 |
| 요청 개수를 손으로 셈 | k6가 일정한 조건으로 측정 |

5주차의 `upstream`·장애 우회 설정은 **하나도 바꾸지 않는다.** 이번 주차에 새로 배우는 것은 **지표를 만들고, 모으고, 보고, 알림받는** 네 단계다.

---

## 0. 실습 공통: 이번 주차의 판정 근거

5주차의 판정 근거는 세 가지였다. 6주차에는 여기에 두 가지가 더해진다.

| 근거 | 확인 대상 | 알 수 있는 것 | 주차 |
| --- | --- | --- | --- |
| `curl`의 상태 코드 | 사용자가 받은 결과 | 서비스가 살아 있는지 | 5주차 |
| Nginx access log의 `upstream` | Nginx가 실제로 보낸 주소 | 어느 앱에 전달했는지, 재시도했는지 | 5주차 |
| 앱별 Tomcat access log | 각 앱이 받은 요청 | 그 앱까지 실제로 도달했는지 | 5주차 |
| **Prometheus의 `up` 지표** | 수집이 성공했는지 | **그 앱이 살아 있는지, 언제부터 죽었는지** | 6주차 |
| **Micrometer의 요청 지표** | 앱이 처리한 요청 수·시간 | **얼마나 처리했고 얼마나 느려졌는지** | 6주차 |

5주차의 세 가지는 **지금 이 순간**만 알려 준다. 6주차의 두 가지는 **시간 축 전체**를 알려 준다. 이 차이가 이번 주차의 핵심이다.

```text
5주차 판정:  명령을 친 그 순간의 사진 한 장
6주차 판정:  장애 전부터 복구 후까지의 동영상
```

### 0-1. 지표는 앱이 만들고, Prometheus가 가져간다

가장 자주 하는 오해부터 정리한다.

```text
오해: Prometheus가 앱을 검사한다.
사실: 앱이 자기 상태를 웹 페이지 하나로 열어 두고, Prometheus가 그 페이지를 읽어 간다.
```

앱이 여는 페이지는 이렇게 생긴 **평범한 텍스트**다.

```text
# HELP http_server_requests_seconds
# TYPE http_server_requests_seconds histogram
http_server_requests_seconds_count{method="GET",status="200",uri="/owners"} 42.0
http_server_requests_seconds_sum{method="GET",status="200",uri="/owners"} 3.821
jvm_memory_used_bytes{area="heap",id="G1 Eden Space"} 5.2428e+07
```

브라우저로 열어도 보이는 그냥 텍스트 문서다. Prometheus는 이 주소를 5초마다 읽어 갈 뿐이다.

### 0-2. 두 앱의 지표를 섞지 않으려면 Nginx를 거치면 안 된다

```text
잘못된 방법:  Prometheus → Nginx:80 → app1 또는 app2 (분산되어 섞임)
올바른 방법:  Prometheus → app1:8080
              Prometheus → app2:8080  (각각 따로 수집)
```

Nginx를 거치면 이번 수집이 app1의 값인지 app2의 값인지 알 수 없다. 그래서 Prometheus는 5주차에서 만든 로드밸런싱을 **일부러 우회해서** 각 앱에 직접 접속한다.

이것이 Prometheus를 `proxy-net`에 붙이는 이유다. `app1`, `app2`라는 이름을 해석할 수 있어야 한다.

---

## 1. 앱이 자기 상태를 숫자로 내놓기

### 1-1. Actuator와 Micrometer

Spring Boot에는 앱의 상태를 밖으로 내보내는 기능이 이미 준비돼 있다. 두 부품으로 나뉘어 있다.

| 부품 | 하는 일 | 비유 |
| --- | --- | --- |
| **Actuator** | `/actuator/**` 주소로 상태를 노출하는 **창구** | 건물의 안내 데스크 |
| **Micrometer** | 요청 수·시간·메모리 등을 실제로 **측정**하는 계측기 | 데스크에 놓인 계기판 |
| **micrometer-registry-prometheus** | 측정값을 **Prometheus가 읽는 형식으로 번역** | 계기판을 Prometheus 언어로 옮겨 적는 사람 |

세 번째가 없으면 `/actuator/prometheus` 주소 자체가 생기지 않는다. 6주차에서 `build.gradle`에 한 줄을 추가하는 이유가 이것이다.

```gradle
runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
```

`runtimeOnly`인 이유는 우리 코드가 이 라이브러리를 직접 호출하지 않기 때문이다. 실행할 때 존재하기만 하면 Spring Boot가 알아서 찾아 쓴다.

### 1-2. 엔드포인트는 기본적으로 닫혀 있다

Actuator의 엔드포인트는 대부분 **기본적으로 공개되지 않는다.** 앱 내부 정보를 아무나 볼 수 있으면 위험하기 때문이다.

```properties
management.endpoints.web.exposure.include=health,info,metrics,prometheus
```

이 줄에 이름을 적은 것만 열린다.

| 엔드포인트 | 보여 주는 것 | 이번 주차에서 쓰는 곳 |
| --- | --- | --- |
| `health` | 앱과 DB가 정상인지 | 기동 완료 판단 |
| `info` | 앱 이름·버전 | 참고용 |
| `metrics` | 지표 목록을 사람이 읽기 좋게 | 어떤 지표가 있는지 둘러볼 때 |
| `prometheus` | 모든 지표를 Prometheus 형식으로 | **Prometheus가 읽어 가는 주소** |

`shutdown` 엔드포인트는 열지 않는다. HTTP 요청 하나로 앱을 종료시킬 수 있어 위험하다. 이번 주차의 장애 실습도 이 방법을 쓰지 않는다.

### 1-3. 지표의 세 가지 종류

Micrometer가 만드는 지표는 성격이 다르다. 이 구분을 모르면 나중에 그래프가 이상하게 보인다.

| 종류 | 성질 | 예 | 어떻게 읽나 |
| --- | --- | --- | --- |
| **Counter** | 증가만 한다. 절대 줄지 않는다 | 총 요청 수 | 값 자체가 아니라 **증가 속도**를 본다 |
| **Gauge** | 오르내린다 | 현재 메모리 사용량, 스레드 수 | 값 자체를 그대로 본다 |
| **Histogram** | 값의 분포를 구간별로 센다 | 응답시간 | 백분위수(p95 등)를 계산해 본다 |

Counter를 그대로 그리면 **계속 우상향하는 직선**만 보인다. 5분 전보다 늘었다는 것 말고는 알 수 없다. 그래서 3절의 `rate()`가 필요하다.

### 1-4. 라벨

같은 이름의 지표라도 조건이 다르면 따로 센다. 이 조건을 **라벨(label)** 이라고 한다.

```text
http_server_requests_seconds_count{method="GET", uri="/owners",     status="200"} 42
http_server_requests_seconds_count{method="GET", uri="/owners/{id}", status="200"} 18
http_server_requests_seconds_count{method="GET", uri="/owners",     status="500"}  3
```

세 줄은 이름이 같지만 서로 다른 시계열이다. 라벨 덕분에 "GET 요청 중 500이 난 것만" 같은 질문을 던질 수 있다.

`uri` 라벨이 `/owners/6`이 아니라 `/owners/{id}`인 점에 주목한다. 실제 주소가 아니라 **경로 패턴**으로 묶는다. 소유자가 1000명이면 시계열이 1000개로 늘어나 저장 공간이 폭발하기 때문이다. 이것을 카디널리티(cardinality) 문제라고 하며, 지표 설계에서 가장 조심하는 부분이다.

### 1-5. 응답시간 히스토그램은 따로 켜야 한다

응답시간의 p95를 보려면 히스토그램 버킷이 필요한데, 기본값으로는 만들어지지 않는다.

```properties
management.metrics.distribution.percentiles-histogram.http.server.requests=true
```

이 설정을 켜면 지표가 다음처럼 구간별로 나온다.

```text
http_server_requests_seconds_bucket{le="0.05"}  12    ← 50ms 이하로 끝난 요청 12건
http_server_requests_seconds_bucket{le="0.1"}   35    ← 100ms 이하 35건
http_server_requests_seconds_bucket{le="0.5"}   48    ← 500ms 이하 48건
http_server_requests_seconds_bucket{le="+Inf"}  50    ← 전체 50건
```

`le`는 "less than or equal", 즉 "이 시간 이하"라는 뜻이다. 각 버킷은 **누적값**이다. 이 계단 모양 데이터로 "전체의 95%가 어느 시간 안에 들어왔는가"를 계산한다.

버킷을 켜면 시계열 수가 늘어난다. 그래서 범위를 제한한다.

```properties
management.metrics.distribution.minimum-expected-value.http.server.requests=10ms
management.metrics.distribution.maximum-expected-value.http.server.requests=10s
```

### 1-6. 왜 평균이 아니라 p95인가

응답시간을 평균으로 보면 문제를 놓친다.

```text
요청 100건 중
 95건이 100ms,  5건이 5000ms

평균 = (95×100 + 5×5000) / 100 = 345ms   → "괜찮네"
p95  = 100ms                              → 95%는 빨랐고
p99  = 5000ms                             → 상위 1%가 5초를 기다렸다
```

평균은 느린 소수를 다수가 희석해 버린다. 그런데 **화가 나서 이탈하는 사용자는 그 느린 소수**다. 그래서 실무에서는 평균 대신 p95, p99를 본다.

이번 주차에는 p95를 기준으로 삼는다. 앱 한 대가 두 대 몫을 처리할 때 p95가 얼마나 오르는지가 관찰 대상이다.

---

## 2. Prometheus가 그 숫자를 모으기

### 2-1. Pull 방식

지표를 모으는 방법은 크게 두 가지다.

| 방식 | 흐름 | 대표 도구 |
| --- | --- | --- |
| Push | 앱이 수집 서버로 **보낸다** | StatsD, Graphite |
| **Pull** | 수집 서버가 앱에서 **가져간다** | **Prometheus** |

Prometheus는 Pull이다. 이 선택이 만드는 성질이 몇 가지 있다.

| 성질 | 이유 |
| --- | --- |
| 앱은 수집 서버 주소를 몰라도 된다 | 앱은 페이지만 열어 두면 되고, 누가 읽어 가는지 신경 쓰지 않는다 |
| **앱이 죽으면 수집이 실패한다** | 이 실패 자체가 곧 장애 신호가 된다 |
| 수집 대상 목록을 수집 서버가 관리한다 | 앱을 늘리면 Prometheus 설정에 주소를 추가해야 한다 |

두 번째 성질이 이번 주차의 핵심 장치다.

### 2-2. `up` 지표는 앱이 만든 것이 아니다

```text
Prometheus가 app1:8080/actuator/prometheus 를 읽으러 감
  ├─ 성공 → up{app="app1"} = 1  로 기록
  └─ 실패 → up{app="app1"} = 0  로 기록
```

`up`은 **Prometheus가 스스로 만드는 지표**다. 앱이 내놓는 게 아니다. 그래서 앱이 완전히 죽어서 아무 응답도 못 하는 상황에서도 `up = 0`이라는 기록이 남는다.

이것이 "앱이 죽었다"를 지표로 표현하는 가장 기본적인 방법이다. 5주차에서는 `curl`로 확인해야 알 수 있던 것을, 이제는 **묻지 않아도 5초마다 기록**된다.

### 2-3. 수집 대상과 라벨 붙이기

```yaml
scrape_configs:
  - job_name: petclinic
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["app1:8080"]
        labels:
          app: app1
      - targets: ["app2:8080"]
        labels:
          app: app2
```

| 구성 | 의미 |
| --- | --- |
| `job_name: petclinic` | 이 수집 묶음의 이름. 모든 지표에 `job="petclinic"` 라벨이 붙는다 |
| `metrics_path` | 읽어 갈 주소의 경로. Spring Boot는 `/actuator/prometheus` |
| `targets` | 읽어 갈 대상의 `호스트:포트` |
| `labels.app` | 이 대상에서 온 지표에 `app="app1"` 라벨을 붙인다 |

`app` 라벨을 여기서 붙이는 이유가 있다. 앱 쪽 설정으로 붙일 수도 있지만, 그러면 **앱을 다시 빌드해야** 이름을 바꿀 수 있다. Prometheus 설정에 두면 파일 한 줄만 고치면 된다.

`app1`, `app2`는 Compose 서비스명이다. 5주차의 `upstream`에서 쓴 이름과 같다. Docker 내부 DNS가 이 이름을 IP로 바꿔 준다.

### 2-4. 수집 간격

```yaml
global:
  scrape_interval: 5s
```

실습에서는 5초로 짧게 잡는다. 장애를 빨리 보여 주기 위해서다.

| 간격 | 장애 감지까지 | 저장 공간 | 앱 부하 |
| --- | --- | --- | --- |
| 5s (실습) | 최대 5초 | 많음 | 조금 있음 |
| 15s (일반적) | 최대 15초 | 보통 | 거의 없음 |
| 60s | 최대 1분 | 적음 | 없음 |

수집 간격이 곧 **감지 지연의 하한**이다. 5초 간격이면 앱이 죽고 나서 최대 5초 뒤에야 `up = 0`이 기록된다. 이 지연을 0으로 만들 수는 없다.

### 2-5. 시계열 데이터

Prometheus가 저장하는 것은 `(시각, 값)` 쌍의 나열이다.

```text
up{app="app1"}:
  10:00:00 → 1
  10:00:05 → 1
  10:00:10 → 0     ← 이 시점에 죽었다
  10:00:15 → 0
  ...
  10:01:05 → 1     ← 이 시점에 살아났다
```

값 하나가 아니라 **나열**이라는 점이 중요하다. 그래서 "지금 살아 있는가"뿐 아니라 **"몇 시 몇 분에 죽어서 몇 초 만에 살아났는가"** 를 나중에 되짚을 수 있다.

이 저장소를 TSDB(Time Series Database)라고 부른다. 실습에서는 보관 기간을 6시간으로 짧게 잡는다. 오래 보관하면 디스크를 계속 먹기 때문이다.

---

## 3. PromQL — 쌓인 숫자에서 답을 꺼내기

문법을 다 배울 필요는 없다. 이번 주차에는 **세 가지**만 알면 된다.

### 3-1. `{ }` — 필터

```promql
up{job="petclinic"}                 job이 petclinic인 것만
up{app="app1"}                      app1의 것만
http_server_requests_seconds_count{status=~"5.."}    status가 5로 시작하는 것만
```

| 연산자 | 뜻 |
| --- | --- |
| `=` | 정확히 같음 |
| `!=` | 같지 않음 |
| `=~` | 정규식과 일치 |
| `!~` | 정규식과 불일치 |

`5..`는 정규식이다. `5`로 시작하고 아무 문자 두 개가 오는 것, 즉 500·502·503 같은 서버 오류를 뜻한다.

### 3-2. `rate()` — 증가 속도로 바꾸기

Counter는 계속 늘기만 하므로 값 자체는 의미가 없다.

```promql
rate(http_server_requests_seconds_count[1m])
```

"최근 1분 동안의 **초당 평균 증가량**"을 뜻한다.

```text
값 그대로:  1000 → 1200 → 1400   "계속 늘어난다" (당연한 말)
rate 적용:   3.3  →  3.3  →  3.3   "초당 3.3건씩 처리 중" (쓸모 있는 말)
```

`[1m]`은 계산에 쓸 구간이다. 짧으면 민감하게 튀고, 길면 완만해진다. 수집 간격이 5초이므로 1분 구간에는 표본이 12개 들어간다.

> `rate()`는 앱이 재시작되어 카운터가 0으로 돌아가도 이를 알아채고 보정한다. 이번 주차처럼 앱이 죽었다 살아나는 상황에서 값이 음수로 튀지 않는 이유다.

### 3-3. `sum by ( )` — 묶어서 더하기

수집된 시계열은 `uri`, `method`, `status`별로 잘게 쪼개져 있다. 이걸 앱 단위로 합친다.

```promql
sum by (app) (rate(http_server_requests_seconds_count[1m]))
```

읽는 순서는 안쪽부터다.

```text
1. http_server_requests_seconds_count   수십 개의 시계열
2. rate(...[1m])                        각각을 초당 증가량으로
3. sum by (app) (...)                   app 라벨이 같은 것끼리 합산
→ 결과: app1 = 10.2 req/s, app2 = 9.8 req/s   두 줄
```

`by (app)`을 빼면 전부 하나로 합쳐져 한 줄이 된다. `by`에 남긴 라벨만 살아남는다고 기억하면 된다.

### 3-4. 이번 주차에 쓰는 쿼리 여섯 개

| 보고 싶은 것 | 쿼리 |
| --- | --- |
| app이 살아 있나 | `up{job="petclinic"}` |
| 초당 요청 수 | `sum by (app) (rate(http_server_requests_seconds_count{job="petclinic"}[1m]))` |
| p95 응답시간 | `histogram_quantile(0.95, sum by (le, app) (rate(http_server_requests_seconds_bucket{job="petclinic"}[1m])))` |
| 5xx 비율 | `sum(rate(http_server_requests_seconds_count{job="petclinic", status=~"5.."}[1m])) / sum(rate(http_server_requests_seconds_count{job="petclinic"}[1m]))` |
| JVM 힙 사용량 | `jvm_memory_used_bytes{job="petclinic", area="heap"}` |
| DB 커넥션 사용 수 | `hikaricp_connections_active{job="petclinic"}` |

p95 쿼리만 조금 복잡하다. 안쪽부터 읽으면 이렇다.

```text
1. ..._bucket[1m]           구간별 누적 개수의 최근 1분치
2. rate(...)                초당 증가량으로
3. sum by (le, app)         버킷 경계(le)와 앱별로 합산   ← le를 반드시 남겨야 한다
4. histogram_quantile(0.95, ...)   그 분포에서 95% 지점을 계산
```

3번에서 `le`를 빠뜨리면 버킷 구분이 사라져 계산이 불가능해진다. p95 쿼리에서 가장 흔한 실수다.

---

## 4. Grafana가 보여 주기

### 4-1. Grafana는 아무것도 측정하지 않는다

```text
PetClinic  →  지표를 만든다
Prometheus →  가져와서 저장한다
Grafana    →  저장된 것을 조회해서 그린다
```

Grafana는 자기 데이터를 하나도 갖고 있지 않다. 화면을 그릴 때마다 Prometheus에 PromQL을 날려 결과를 받아 온다. 패널 하나를 열어 보면 3절에서 본 쿼리가 그대로 들어 있다.

Prometheus를 끄면 Grafana의 모든 패널이 빈다. 반대로 Grafana를 꺼도 Prometheus는 계속 수집한다. 나중에 Grafana를 켜면 그동안의 기록이 그대로 보인다. **저장은 Prometheus의 일이기 때문이다.**

### 4-2. 프로비저닝

Grafana는 화면에서 클릭으로 설정할 수도 있고, 파일로 설정할 수도 있다. 실습에서는 파일을 쓴다.

| 방식 | 장점 | 단점 |
| --- | --- | --- |
| 화면에서 클릭 | 처음 배울 때 직관적 | 컨테이너를 지우면 사라진다. 다른 사람과 공유하기 어렵다 |
| **파일 프로비저닝** | **설정이 파일로 남아 그대로 재현된다** | 형식을 지켜야 한다 |

파일로 두면 `docker compose down` 후 다시 올려도 대시보드와 경고가 그대로 살아난다. 스터디에서 모두가 같은 화면을 보려면 이 방식이 맞다.

| 파일 | 정하는 것 |
| --- | --- |
| `provisioning/datasources/*.yml` | 어느 Prometheus에 질의할지 |
| `provisioning/dashboards/*.yml` | 대시보드 파일을 어느 폴더에서 읽을지 |
| `dashboards/*.json` | 실제 패널과 쿼리 |
| `provisioning/alerting/*.yml` | 경고 규칙 |

데이터소스에 `uid: prometheus`처럼 **고정 식별자**를 주는 것이 중요하다. 대시보드와 경고 규칙이 이 식별자로 데이터소스를 참조하기 때문이다. 자동 생성에 맡기면 매번 값이 달라져 참조가 깨진다.

### 4-3. 이번 주차 대시보드의 일곱 패널

| 패널 | 보는 지표 | 장애 때 어떻게 변하는가 |
| --- | --- | --- |
| app 상태 | `up` | app1이 UP → DOWN → UP |
| 초당 요청 수 | 요청 Counter의 rate | app1은 0으로, app2는 약 2배로 |
| 5xx 오류율 | 5xx 요청 비율 | **0 근처를 유지해야 한다** |
| p95 / p50 응답시간 | 히스토그램 | 조금 오른다 |
| JVM 힙 | Gauge | app2 쪽이 늘어난다 |
| JVM 스레드 수 | Gauge | app2 쪽이 늘어난다 |
| DB 커넥션 풀 | Gauge | app2 쪽이 늘어난다 |

세 번째가 오늘의 성적표다. **오류율이 0 근처를 유지했다면 사용자는 장애를 겪지 않았다는 뜻**이다. 나머지 패널은 "그 사이 내부에서 무슨 일이 있었는가"를 설명한다.

---

## 5. 사람이 안 보고 있어도 알기 — 경고

### 5-1. 경고 규칙의 구조

Grafana의 경고 규칙은 세 단계로 되어 있다.

```text
A: 쿼리         up{job="petclinic"}       → 시계열 (여러 시점의 값)
B: 축약(reduce) last                      → 값 하나로 (마지막 값)
C: 비교(threshold) < 1                    → 참/거짓
```

쿼리 결과는 여러 시점의 값이 나열된 형태라 그대로는 "참인가 거짓인가"를 판정할 수 없다. 그래서 값 하나로 줄인 뒤 임계값과 비교한다. `condition: C`는 "C의 결과로 판정한다"는 뜻이다.

### 5-2. 상태 전이

경고는 두 상태가 아니라 세 상태를 오간다.

```text
Normal  ──조건 참──▶  Pending  ──for 시간 경과──▶  Alerting
   ▲                     │                            │
   └────조건 거짓─────────┴────────────────────────────┘
```

| 상태 | 의미 |
| --- | --- |
| `Normal` | 조건이 거짓. 정상 |
| `Pending` | 조건이 참이 됐지만 아직 `for` 시간을 못 채움 |
| `Alerting` | `for` 시간 동안 계속 참이었음. 실제 알림 발송 |

`Pending`이 있는 이유는 **일시적인 튐으로 알림이 울리는 것을 막기 위해서**다. 수집이 한 번 실패했다고 바로 알리면, 새벽에 의미 없는 알림으로 깨는 일이 생긴다.

```yaml
for: 20s
```

이번 실습에서는 20초로 짧게 잡는다. 실제 운영에서는 보통 1~5분을 쓴다.

### 5-3. `noDataState`

값이 아예 없을 때 어떻게 처리할지도 정해야 한다.

| 설정 | 동작 | 언제 쓰나 |
| --- | --- | --- |
| `NoData` | 별도의 "데이터 없음" 상태 | 원래 간헐적인 지표 |
| `Alerting` | 장애로 간주 | **항상 있어야 하는 지표** |
| `OK` | 정상으로 간주 | 거의 쓰지 않는다 |

`up` 지표에는 `Alerting`이 맞다. `up`은 항상 존재해야 하는 값이므로, 그게 없다는 것은 Prometheus 자체에 문제가 생겼다는 뜻이기 때문이다. "데이터가 없으니 조용히 있자"는 가장 위험한 선택이다.

### 5-4. 알림을 어디로 보낼 것인가

Grafana는 Slack·이메일·Webhook 등으로 알림을 보낼 수 있다. 이번 실습에서는 **보내지 않는다.** 외부 서비스 연동은 계정과 토큰이 필요해 스터디 시간에 하기에 적절하지 않다.

대신 화면에서 **상태가 Normal → Pending → Alerting → Normal로 바뀌는 것**을 확인한다. 알림 발송은 이 상태 전이에 붙는 부가 기능일 뿐, 판정 자체는 이미 여기서 끝난다.

---

## 6. 같은 조건으로 비교하기 — k6

### 6-1. 왜 `curl` 반복으로는 부족한가

5주차에서는 이렇게 요청을 보냈다.

```bash
for i in $(seq 1 10); do curl ... "http://localhost:8080/owners?study=week5-$i"; done
```

이 방법은 "분산되는가"를 확인하는 데는 충분하다. 하지만 성능을 비교하기에는 부족하다.

| 문제 | 이유 |
| --- | --- |
| 요청이 하나씩 순서대로 나간다 | 앞 요청이 끝나야 다음이 나가므로, **서버가 느려지면 요청 수도 줄어든다** |
| 응답시간을 모아 주지 않는다 | 매번 눈으로 봐야 하고 p95 같은 통계가 없다 |
| 실행할 때마다 조건이 다르다 | 타이핑 속도, 네트워크 상태에 따라 달라진다 |

첫 번째가 특히 문제다. 서버가 느려지면 요청도 자동으로 줄어들어, **장애 상황에서 부하가 오히려 가벼워진다.** 이러면 비교 자체가 성립하지 않는다.

### 6-2. constant-arrival-rate

k6에는 여러 실행 방식이 있다. 이번 주차에는 `constant-arrival-rate`를 쓴다.

| 방식 | 기준 | 서버가 느려지면 |
| --- | --- | --- |
| `constant-vus` | 동시 사용자 수를 고정 | 요청 수가 **줄어든다** |
| **`constant-arrival-rate`** | **초당 요청 수를 고정** | 요청 수를 **유지한다** (동시 사용자를 늘려서) |

```javascript
scenarios: {
  steady: {
    executor: 'constant-arrival-rate',
    rate: 20,             // 초당 20건
    timeUnit: '1s',
    duration: '2m',
    preAllocatedVUs: 20,
    maxVUs: 80,
  },
}
```

초당 20건을 **무슨 일이 있어도** 유지한다. 그래야 "같은 부하에서 장애 전후가 어떻게 달랐는가"를 말할 수 있다.

`maxVUs`가 필요한 이유는, 서버가 느려지면 요청을 유지하기 위해 동시 사용자를 계속 늘려야 하기 때문이다. 상한이 없으면 시험 도구가 먼저 자원을 다 쓴다.

### 6-3. 왜 `/owners`인가

5주차 4-2에서 확인한 내용이 그대로 적용된다.

| 경로 | 문제 |
| --- | --- |
| `/` (메인) | 정적 리소스 위주. Nginx 캐시에서 끝나 앱까지 가지 않는다 |
| `/resources/images/*.png` | 캐시 `HIT`이면 앱 지표에 아무것도 안 잡힌다 |
| **`/owners`** | **DB 조회가 들어가는 동적 경로. 캐시되지 않고 앱 부하가 실제로 잡힌다** |

캐시된 요청은 Nginx가 직접 응답하므로 로드밸런싱과도, 앱 성능과도 무관하다. 5주차의 판정 조합표 마지막 줄(`cache=HIT`)이 여기서도 그대로 유효하다.

### 6-4. 5주차의 `?study=` 표식을 그대로 쓴다

k6 요청에도 `?study=week6-...` 표식을 붙인다. 그러면 5주차에서 쓰던 명령으로 앱별 처리 건수를 그대로 셀 수 있다.

```bash
grep -Rc "study=week6-load" logs/tomcat/app1
grep -Rc "study=week6-load" logs/tomcat/app2
```

Prometheus의 지표와 Tomcat 로그의 개수가 **서로 맞는지 대조**할 수 있다는 점이 중요하다. 새 도구의 숫자를 믿기 전에, 이미 검증된 방법과 맞춰 보는 것이다.

### 6-5. 기록할 값

| 값 | 뜻 |
| --- | --- |
| 전체 요청 수 | 시험 동안 보낸 총 건수 |
| 초당 요청 수 | 실제로 달성된 속도 |
| 평균 응답시간 | 참고용 |
| **p95 응답시간** | **95%의 요청이 이 시간 안에 끝났다** |
| **HTTP 실패율** | **사용자가 겪은 실패의 비율** |
| app1·app2 처리 건수 | 분산이 유지됐는지 |

굵게 표시한 두 개가 장애 전후 비교의 핵심이다.

---

## 7. 죽은 앱을 자동으로 살리기 — Restart Policy

### 7-1. 네 가지 정책

Docker는 컨테이너의 주 프로세스가 종료됐을 때의 행동을 정할 수 있다.

| 정책 | 동작 |
| --- | --- |
| `no` (기본값) | 아무것도 하지 않는다 |
| `on-failure` | 종료 코드가 0이 아닐 때만 다시 실행 |
| `always` | 항상 다시 실행. 사용자가 멈춰도 데몬 재시작 시 다시 켠다 |
| **`unless-stopped`** | **항상 다시 실행. 단 사용자가 명시적으로 멈춘 것은 제외** |

이번 주차에는 `unless-stopped`를 쓴다.

```yaml
app1:
  restart: unless-stopped
```

"~하지 않는 한(unless)"이 가리키는 것은 **사용자가 의도적으로 멈춘 경우**다. 이 구분이 5주차 실습과 6주차 실습을 나눈다.

```text
docker compose stop app1   → 사용자가 멈춤    → 다시 실행하지 않음  (5주차 실습)
docker compose kill app1   → 사용자가 멈춤    → 다시 실행하지 않음
컨테이너 안에서 프로세스가 죽음 → 사고         → 다시 실행함        (6주차 실습)
```

사용자가 멈춘 것까지 되살리면 컨테이너를 멈출 방법이 없어진다. 그래서 이 예외가 필요하다.

두 번째 줄을 눈여겨봐야 한다. **`kill`도 사용자 중지에 들어간다.** 이름 때문에 "강제로 죽이는 것"으로 보이지만, Docker 입장에서 `docker kill`은 `docker stop`과 마찬가지로 **사용자가 데몬에게 보낸 중지 요청**이다. 보내는 신호가 SIGKILL이든 SIGTERM이든 다르지 않다.

그래서 자동 재시작을 가르는 기준은 **어떤 신호를 보냈는가**가 아니라 **누가 죽였는가**다.

| 누가 죽였나 | Docker의 해석 | 자동 재시작 |
| --- | --- | --- |
| 사용자가 데몬 API로 요청 (`stop`, `kill`) | 의도된 중지 | ✕ |
| 컨테이너 안의 프로세스가 죽음 (크래시, OOM) | 사고 | ○ |

### 7-2. Healthcheck와는 다르다

두 기능은 이름이 비슷해 자주 섞인다. 하는 일이 완전히 다르다.

| | Healthcheck | Restart Policy |
| --- | --- | --- |
| 하는 일 | 컨테이너가 정상인지 **판정** | 프로세스가 죽었을 때 **다시 실행** |
| 결과 | 상태 표시 (`healthy` / `unhealthy`) | 컨테이너 재실행 |
| 성격 | 판단 | 행동 |
| 쓰이는 곳 | `depends_on: condition: service_healthy` | 자동 복구 |

핵심 차이는 이것이다.

```text
Healthcheck    : "살아 있지만 응답을 못 한다"를 알아낼 수 있다. 하지만 알아내기만 한다.
Restart Policy : "완전히 죽었다"에만 반응한다. 하지만 실제로 되살린다.
```

**Docker의 기본 동작에서는 `unhealthy` 판정이 나도 컨테이너를 다시 실행하지 않는다.** 앱이 살아는 있는데 먹통이 된 상태(hang)는 이번 구조로는 자동 복구되지 않는다. 이 한계는 Kubernetes의 livenessProbe 같은 기능이 해결한다.

### 7-3. 어떻게 "비정상 종료"를 만들 것인가

앞 절의 기준을 그대로 적용하면, 밖에서 컨테이너를 멈추는 명령은 전부 쓸 수 없다.

| 명령 | 성격 | 자동 재시작 |
| --- | --- | --- |
| `docker compose stop app1` | 사용자 중지 | ✕ (5주차 실습에 사용) |
| `docker compose pause app1` | 프로세스 일시정지. 종료가 아님 | ✕ (5주차 7절에 사용) |
| `docker compose kill -s SIGKILL app1` | **사용자 중지.** 신호와 무관하다 | ✕ |
| **`docker compose exec app1 sh -c 'kill -9 $(pgrep -f "java -jar")'`** | **컨테이너 안에서 앱이 죽음** | **○ (6주차 실습에 사용)** |

마지막 줄이 6주차의 장애 주입 방법이다. **컨테이너 밖에서 컨테이너를 멈추는 것이 아니라, 컨테이너 안으로 들어가 앱 프로세스를 죽인다.** Docker가 보기에는 앱이 스스로 죽은 것이므로 사고로 판정하고 다시 실행한다.

`SIGKILL`(`kill -9`)을 쓰는 이유는 **전원이 갑자기 나간 것에 가장 가까운 상황**이기 때문이다. 앱이 정리할 틈도 없이 사라진 상태를 재현한다.

| 신호 | 뜻 | JVM의 반응 | 종료 코드 |
| --- | --- | --- | --- |
| `SIGTERM` (기본) | "정리하고 끝내라" | 종료 훅 실행 후 정상 종료 | 143 |
| **`SIGKILL`** | **"즉시 죽어라"** | **반응할 수 없음. 그대로 죽는다** | **137** |

#### `init: true`가 필요한 이유

컨테이너 안에서 앱을 죽이려면 Compose에 한 줄이 더 필요하다.

```yaml
app1:
  restart: unless-stopped
  init: true          # ← 이것이 없으면 앱을 죽일 수 없다
```

이유는 리눅스 커널의 동작에 있다. **PID 1은 특별 취급을 받아, 자기가 핸들러를 등록하지 않은 신호를 같은 네임스페이스 안에서 받으면 무시한다.** SIGKILL도 예외가 아니다. 컨테이너에서 JVM은 기본적으로 PID 1이므로, 안에서 `kill -9`를 보내도 아무 일이 일어나지 않는다.

`init: true`를 넣으면 Docker가 `docker-init`이라는 작은 프로세스를 PID 1로 먼저 띄우고, JVM은 그 자식(PID 2 이하)이 된다. PID 1이 아니게 된 JVM은 평범한 프로세스이므로 `kill -9`로 죽는다.

```text
init 없음 : PID 1 = java          → kill -9 무시됨. 죽지 않는다
init: true : PID 1 = docker-init
             PID 2 = java          → kill -9 로 죽는다 → Docker가 재시작
```

이 한 줄이 빠지면 4단계에서 앱을 죽여도 아무 일이 일어나지 않고, 원인을 찾느라 오래 헤매게 된다.

### 7-4. 복구에 걸리는 시간

자동 복구는 즉시 끝나지 않는다.

```text
app1의 JVM 강제 종료
  ↓  즉시
컨테이너 종료 (Exited)
  ↓  약 1초
Docker가 컨테이너 다시 실행 (Up)
  ↓  20~60초  ← Spring Boot 기동 시간
PetClinic이 요청을 받을 준비 완료
  ↓  최대 5초  ← Prometheus 수집 간격
up{app="app1"} = 1 로 기록
  ↓  최대 10초 ← 5주차의 fail_timeout
Nginx가 app1을 다시 분산 대상으로 사용
```

컨테이너가 `Up`으로 보인다고 서비스가 복구된 것이 아니다. **`Up`은 프로세스가 시작됐다는 뜻일 뿐**이고, 앱이 요청을 받기까지는 시간이 더 걸린다. 이 구간을 눈으로 확인하는 것이 이번 실습의 관찰 포인트 중 하나다.

`RestartCount`로 Docker가 실제로 되살렸다는 증거를 남길 수 있다.

```bash
docker inspect -f '{{.State.Status}} restarts={{.RestartCount}}' <컨테이너>
```

---

## 8. 누가 복구하고 누가 관측하는가

이번 주차에서 가장 중요한 구분이다. 도구 다섯 개가 나오지만 **역할이 두 갈래로 완전히 나뉜다.**

| 역할 | 도구 | 하는 일 |
| --- | --- | --- |
| **복구** | Nginx | 요청이 실패하면 다른 앱으로 우회 |
| **복구** | Docker Restart Policy | 죽은 컨테이너를 다시 실행 |
| **관측** | Micrometer | 앱의 상태를 숫자로 만든다 |
| **관측** | Prometheus | 그 숫자를 수집·저장 |
| **관측** | Grafana | 저장된 숫자를 표시하고 경고 |

두 갈래는 **서로 연결되어 있지 않다.**

```text
Prometheus가 app1의 장애를 감지했다
  → Nginx에게 알려 주는가?  아니오.
  → Docker에게 재시작하라고 시키는가?  아니오.

Nginx는 app1이 죽은 것을 어떻게 알았나?
  → Prometheus가 알려 줘서?  아니오.
  → 자기가 보낸 요청이 실패해서.  (5주차의 수동 감지)
```

### 8-1. 이렇게 나뉘어 있는 것이 왜 좋은가

| 이유 | 설명 |
| --- | --- |
| 관측이 죽어도 서비스는 산다 | Prometheus를 꺼도 Nginx의 우회와 Docker의 재시작은 그대로 동작한다 |
| 관측을 마음대로 바꿀 수 있다 | 대시보드를 고쳐도 서비스 동작에 영향이 없다 |
| 각자 잘하는 일을 한다 | 우회 판단은 밀리초 단위, 관측은 초 단위로 충분하다 |

만약 Nginx가 Prometheus의 판단을 기다렸다면, 수집 간격 5초 + 평가 시간만큼 우회가 늦어졌을 것이다. 사용자는 그동안 오류 화면을 봤을 것이다.

### 8-2. 그럼 관측은 왜 하는가

복구에 관여하지 않는다면 관측이 왜 필요한가. 세 가지 답이 있다.

```text
1. 복구가 됐는지 확인하기 위해   → 정말 자동으로 살아났는가?
2. 사용자가 영향을 받았는지 알기 위해 → 오류율은? 응답시간은?
3. 나중에 되짚기 위해            → 새벽 3시에 무슨 일이 있었는가?
```

세 번째가 특히 중요하다. 자동 복구가 잘 되면 **아무도 장애를 눈치채지 못한다.** 그건 좋은 일이지만, 동시에 "왜 죽었는가"를 조사할 기회를 잃는 것이기도 하다. 지표가 남아 있어야 원인을 찾을 수 있다.

```text
지표가 없는 자동 복구:  "어제 서버에 무슨 일 있었나?"  "모르겠는데요."
지표가 있는 자동 복구:  "새벽 3시 12분에 app1이 죽어서 41초 만에 복구됐고,
                        그 사이 요청 실패는 2건이었습니다."
```

---

## 참고: 이 구조가 아직 해결하지 못한 것

| 항목 | 현재 상태 | 실제 운영에서의 접근 |
| --- | --- | --- |
| 앱이 살아 있으면서 먹통 | Restart Policy가 감지하지 못한다 | Kubernetes의 livenessProbe로 실패 시 재시작 |
| Prometheus 자신의 장애 | 한 대. 멈추면 그동안의 지표가 없다 | 이중화, 원격 저장소(remote write) |
| Grafana 자신의 장애 | 한 대. 멈추면 화면을 볼 수 없다 | 이중화. 다만 지표는 Prometheus에 남는다 |
| 로그 | 컨테이너 로그를 사람이 본다 | Loki·Elasticsearch로 로그도 수집 |
| 요청 추적 | 한 요청이 어디서 느려졌는지 모른다 | 분산 추적(OpenTelemetry, Jaeger) |
| 알림 발송 | 화면의 상태 전이만 확인 | Slack·PagerDuty 연동, 당직 순번 |
| 지표 보관 | 6시간 | 장기 저장소로 수개월~수년 보관 |

첫 줄이 흥미로운 지점이다. 오늘 만든 자동 복구는 **완전히 죽은 경우에만** 동작한다. 실제 장애에서 더 흔한 것은 5주차 7절에서 본 것처럼 **살아 있으면서 응답만 못 하는 상태**다. 이 경우를 자동으로 처리하려면 "정기적으로 검사해서 실패하면 죽여 버리고 다시 띄우는" 장치가 필요하고, 그것이 컨테이너 오케스트레이터가 하는 일 중 하나다.

Nginx의 수동 감지, Docker의 재시작 정책, Prometheus의 관측을 각각 따로 붙여 본 지금이라면, 그런 도구가 무엇을 통합하려는 것인지 짐작할 수 있다.

## 참고 자료

- [Prometheus 개요](https://prometheus.io/docs/introduction/overview/)
- [Prometheus 데이터 모델](https://prometheus.io/docs/concepts/data_model/)
- [PromQL 기본](https://prometheus.io/docs/prometheus/latest/querying/basics/)
- [Spring Boot Actuator Metrics](https://docs.spring.io/spring-boot/reference/actuator/metrics.html)
- [Micrometer Prometheus 레지스트리](https://docs.micrometer.io/micrometer/reference/implementations/prometheus.html)
- [Grafana 경고 규칙 파일 프로비저닝](https://grafana.com/docs/grafana/latest/alerting/set-up/provision-alerting-resources/file-provisioning/)
- [Grafana k6 실행 방식(executors)](https://grafana.com/docs/k6/latest/using-k6/scenarios/executors/)
- [Docker 컨테이너 자동 시작 정책](https://docs.docker.com/engine/containers/start-containers-automatically/)
- 『이것이 리눅스다』 4.6 프로세스·서비스, 20.2 도커의 개념과 실습
