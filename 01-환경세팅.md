# 01. 환경 세팅 — 6주차 시작 전 확인

5주차와 **같은 PetClinic 프로젝트 루트**에서 진행합니다. 6주차는 앱 구성을 바꾸지 않고, 5주차에서 만든 `app1`·`app2` 구조 **옆에 관측 도구를 붙이는** 시간입니다.

5주차와 달라지는 점이 하나 있습니다. 이번 주차에는 **새 이미지 세 개**를 받고, **PetClinic 이미지를 다시 빌드**합니다. 앱이 지표를 내놓게 하려면 라이브러리를 하나 추가해야 하기 때문입니다.

| 구분 | 5주차 | 6주차 |
| --- | --- | --- |
| 새로 받을 이미지 | 없음 | `prom/prometheus`, `grafana/grafana`, `grafana/k6` |
| PetClinic 이미지 | 4주차 것 그대로 사용 | **다시 빌드** (의존성 추가 때문) |
| 호스트에 여는 포트 | `8080` 하나 | `8080`, `9090`, `3000` 세 개 |
| 수정할 파일 | `docker-compose.yml`, `default.conf` | `build.gradle`, `application.properties`, `docker-compose.yml` |

## 1. 모임 전 확인할 것

아래만 확인되면 시작할 수 있습니다.

```bash
docker version
docker compose version
curl --version
command -v grep awk seq
docker compose config
docker compose ps
```

| 확인 | 정상 기준 | 6주차에서 필요한 이유 |
| --- | --- | --- |
| Docker | `docker version`에 Client·Server 정보가 표시 | Nginx·app1·app2·PostgreSQL에 Prometheus·Grafana 추가 실행 |
| Compose | 버전 표시 | 일곱 서비스를 한 구성으로 관리 |
| 기존 Compose | `config`, `ps`가 동작 | 5주차 구성을 이어서 사용 |
| curl | 버전 표시 | 지표 엔드포인트와 Prometheus API 확인 |
| grep·seq | 각 명령의 경로 표시 | 5주차와 같은 방식으로 앱별 처리 횟수 세기 |

Docker Server 연결 오류가 나면 Docker Desktop을 실행합니다. WSL2 사용자는 **Settings → Resources → WSL Integration → Ubuntu**도 켭니다.

> `docker compose down -v`는 실행하지 않습니다. PostgreSQL Named Volume까지 지워 기존 데이터가 사라집니다.

### 메모리 확인

이번 주차에는 컨테이너가 네 개에서 여섯 개로 늘어납니다. Docker Desktop에 할당된 메모리가 부족하면 앱이 기동 중에 죽습니다.

```bash
# Docker에 할당된 전체 메모리를 확인한다.
docker info --format '{{.MemTotal}}' | awk '{printf "%.1f GB\n", $1/1024/1024/1024}'
```

**4GB 이상**을 권장합니다. 그보다 적으면 Docker Desktop의 **Settings → Resources → Memory**에서 올립니다.

## 2. 5주차 결과물이 남아 있는지 확인

6주차는 5주차의 최종 상태에서 시작합니다. 아래 다섯 가지가 있어야 합니다.

```bash
# 5주차에서 나눈 app1·app2 서비스가 정의돼 있는지 확인한다.
docker compose config --services

# Nginx가 upstream으로 두 앱을 묶고 있는지 확인한다.
grep -n -E 'upstream petclinic_backend|server app[12]:8080' nginx/conf.d/default.conf

# 두 proxy_pass가 모두 upstream 이름을 가리키는지 개수로 확인한다.
grep -c 'proxy_pass http://petclinic_backend;' nginx/conf.d/default.conf

# 앱별 Tomcat 로그 폴더가 있는지 확인한다.
ls -ld logs/tomcat/app1/access logs/tomcat/app2/access

# PetClinic 이미지가 남아 있는지 확인한다.
docker image inspect spring-petclinic:dockerfile >/dev/null && echo 이미지있음
```

| 확인할 것 | 정상 기준 | 없으면 |
| --- | --- | --- |
| Compose 서비스 | `app1`, `app2`, `nginx`, `postgres` 네 개 | 5주차 1-2를 다시 적용 |
| `upstream petclinic_backend` | `server app1:8080`, `server app2:8080` 두 줄 | 5주차 2-2를 다시 적용 |
| `proxy_pass` 개수 | `2` | 5주차 2-2를 다시 적용 |
| 앱별 로그 폴더 | 두 폴더 모두 존재 | 5주차 01-환경세팅 5절을 다시 적용 |
| PetClinic 이미지 | `이미지있음` 출력 | 아래 명령으로 다시 빌드 |

이미지가 없으면 다시 만듭니다.

```bash
./gradlew bootJar
docker build -t spring-petclinic:dockerfile .
```

> 이 이미지는 6주차 2절에서 **어차피 한 번 더 빌드합니다.** 지금은 5주차 상태가 정상인지 확인하는 용도이므로, 이미지가 있으면 다시 빌드하지 않아도 됩니다.

## 3. 지금 실행할 명령

수정 전 백업을 만들고 현재 상태를 확인합니다.

```bash
# 백업 파일 이름에 붙일 현재 날짜와 시각을 만든다.
STAMP=$(date +%Y%m%d-%H%M)

# 6주차 수정 전 상태로 Compose 파일과 빌드 설정, 앱 설정을 복사한다.
cp docker-compose.yml docker-compose.yml.before-week6-$STAMP
cp build.gradle build.gradle.before-week6-$STAMP
cp src/main/resources/application.properties src/main/resources/application.properties.before-week6-$STAMP

# 백업 파일이 실제로 만들어졌는지 확인한다.
ls -l docker-compose.yml.before-week6-* build.gradle.before-week6-*
ls -l src/main/resources/application.properties.before-week6-*

# 5주차 구성이 실제로 동작하는지 한 번 실행해 확인한다.
docker compose up -d
docker compose ps
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8080/owners?study=week6-pre"
```

마지막 명령이 `200`이면 5주차 상태가 정상이며 6주차를 시작할 수 있습니다.

| 백업 파일 | 6주차에서 이 파일을 바꾸는 이유 |
| --- | --- |
| `docker-compose.yml` | Prometheus·Grafana·k6 서비스 추가, 두 앱에 재시작 정책 추가 |
| `build.gradle` | 지표 라이브러리(Micrometer Prometheus) 의존성 추가 |
| `application.properties` | 지표 엔드포인트 공개와 응답시간 히스토그램 설정 추가 |

> `nginx/conf.d/default.conf`는 이번 주차의 **필수 단계에서는 바꾸지 않습니다.** 선택 단계에서 한 줄만 추가합니다. 5주차 백업이 그대로 남아 있으므로 따로 다시 만들지 않아도 됩니다.

확인이 끝나면 컨테이너를 내립니다. 데이터 볼륨은 유지합니다.

```bash
docker compose down
```

## 4. 6주차에서 사용할 구조 확인

### 바뀌는 부분만 보기

```text
5주차                              6주차
────────────────────────────────  ────────────────────────────────
사용자                             사용자 / k6
  ↓ localhost:8080                   ↓ localhost:8080
Nginx                              Nginx
  ├─ app1:8080                       ├─ app1:8080 ──┐
  └─ app2:8080                       └─ app2:8080 ──┤ 지표 제공
       ↓ postgres:5432                    ↓         │
PostgreSQL                         PostgreSQL       │
                                                    ↓
                                              Prometheus  (수집·저장)
                                                    ↓
                                               Grafana    (표시·경고)
```

**요청이 흐르는 경로는 5주차와 완전히 같습니다.** 옆에 관측 경로가 하나 더 생겼을 뿐입니다.

### 새 서비스가 붙을 네트워크

| 서비스 | 참여할 네트워크 | 이유 |
| --- | --- | --- |
| `nginx` | `proxy-net` | 5주차와 동일 |
| `app1`·`app2` | `proxy-net`, `db-net` | 5주차와 동일 |
| `postgres` | `db-net` | 5주차와 동일 |
| `prometheus` | `proxy-net` | **app1·app2에 직접 접속**해 지표를 수집해야 함 |
| `grafana` | `proxy-net` | Prometheus에 질의해야 함 |
| `k6` | `proxy-net` | Nginx에 요청을 보내야 함 |

Prometheus를 `proxy-net`에 붙이는 이유가 중요합니다. Prometheus는 **Nginx를 거치지 않고** `app1:8080`, `app2:8080`에 각각 직접 접속합니다. Nginx를 거치면 요청이 분산되어 어느 앱의 지표인지 알 수 없게 되기 때문입니다.

`app1`·`app2`에는 여전히 `ports:`를 두지 않습니다. Prometheus는 같은 Docker 네트워크 안에 있으므로 호스트 포트 없이도 `expose: ["8080"]`만으로 접속할 수 있습니다.

### 포트

호스트에 공개하는 포트가 **하나에서 셋으로** 늘어납니다.

```text
브라우저 → 호스트 8080 → Nginx 컨테이너 80 ─┬→ app1 컨테이너 8080
                                            └→ app2 컨테이너 8080
브라우저 → 호스트 9090 → Prometheus 컨테이너 9090
브라우저 → 호스트 3000 → Grafana 컨테이너 3000
```

| 포트 | 서비스 | 여는 이유 |
| --- | --- | --- |
| `8080` | Nginx | 사용자 요청 (5주차와 동일) |
| `9090` | Prometheus | 수집 상태와 저장된 값을 브라우저로 확인 |
| `3000` | Grafana | 대시보드와 경고를 브라우저로 확인 |

`9090`과 `3000`은 **운영이라면 열지 않는 포트**입니다. 실습에서 화면으로 직접 확인하기 위해 여는 것이며, 실제 서비스에서는 내부망에서만 접근하게 합니다.

### 포트 확인

시작 전에 세 포트가 모두 비어 있는지 확인합니다.

**Ubuntu / WSL2 Ubuntu**

```bash
sudo ss -ltnp '( sport = :8080 or sport = :9090 or sport = :3000 )'
```

**macOS**

```bash
sudo lsof -nP -iTCP:8080,9090,3000 -sTCP:LISTEN
```

| 포트 | 자주 충돌하는 프로그램 | 대처 |
| --- | --- | --- |
| `3000` | React·Next.js 개발 서버, 다른 Grafana | 해당 터미널에서 `Ctrl+C`, 또는 Compose에서 `3001:3000`으로 변경 |
| `9090` | 다른 Prometheus, 일부 프록시 도구 | 종료하거나 `9091:9090`으로 변경 |
| `8080` | 로컬 `./gradlew bootRun` | 해당 터미널에서 `Ctrl+C` |

포트를 바꿀 때는 **왼쪽 숫자만** 바꿉니다. 오른쪽은 컨테이너 안의 포트이므로 그대로 둡니다.

## 5. 관측 설정 폴더 만들기

### 왜 폴더를 나누는가

Prometheus와 Grafana는 실행할 때 설정 파일을 읽습니다. 이 파일들을 프로젝트 루트에 흩어 두면 5주차까지 만든 파일과 섞입니다. 그래서 `monitoring` 폴더 하나에 모읍니다.

```text
monitoring/
├── prometheus/
│   └── prometheus.yml                     Prometheus가 무엇을 수집할지
└── grafana/
    ├── provisioning/
    │   ├── datasources/                   Grafana가 어디에 질의할지
    │   ├── dashboards/                    대시보드를 어디서 읽을지
    │   └── alerting/                      경고 규칙
    └── dashboards/                        대시보드 정의 파일
```

Grafana의 폴더가 두 갈래인 것이 헷갈리기 쉽습니다.

| 폴더 | 담는 것 | 성격 |
| --- | --- | --- |
| `provisioning/dashboards/` | "대시보드를 어느 폴더에서 읽어라"는 **지시서** | 설정 |
| `dashboards/` | 실제 **대시보드 내용**(패널·쿼리) | 내용 |

지시서와 내용이 분리되어 있어서, 대시보드를 추가할 때 `dashboards/` 폴더에 파일만 넣으면 Grafana가 자동으로 읽습니다.

### 지금 만들 폴더

프로젝트 루트에서 실행합니다.

```bash
# Prometheus와 Grafana의 설정 파일을 담을 폴더를 한 번에 만든다.
mkdir -p monitoring/prometheus \
         monitoring/grafana/provisioning/datasources \
         monitoring/grafana/provisioning/dashboards \
         monitoring/grafana/provisioning/alerting \
         monitoring/grafana/dashboards \
         k6

# 만들어진 구조를 확인한다.
find monitoring k6 -type d | sort
```

| 명령 | 하는 일 |
| --- | --- |
| `mkdir -p` | 상위 폴더가 없어도 함께 만들고, 이미 있어도 오류를 내지 않는다 |
| `\` (줄 끝) | 명령이 다음 줄로 이어진다는 표시 |
| `find ... -type d` | 폴더만 골라 목록으로 출력한다 |

`k6` 폴더는 부하 시험 스크립트를 담을 곳입니다.

> Prometheus와 Grafana의 **데이터**는 폴더가 아니라 Docker Named Volume에 저장합니다. 호스트 폴더로 연결하면 컨테이너 안의 사용자 권한 문제로 시작에 실패하는 경우가 있기 때문입니다. Compose 파일에서 `prometheus-data`, `grafana-data` 볼륨으로 지정합니다.

## 6. 이미지 미리 받아 두기

모임 당일에 이미지를 받으면 네트워크 상황에 따라 오래 걸립니다. 미리 받아 둡니다.

```bash
# 이번 주차에 사용할 세 이미지를 미리 내려받는다.
docker pull prom/prometheus:v3.1.0
docker pull grafana/grafana:11.5.1
docker pull grafana/k6:latest

# 세 이미지가 모두 받아졌는지 확인한다.
docker images | grep -E 'prometheus|grafana|k6'
```

| 이미지 | 크기(대략) | 역할 |
| --- | --- | --- |
| `prom/prometheus:v3.1.0` | 약 250MB | 지표 수집·저장 |
| `grafana/grafana:11.5.1` | 약 450MB | 대시보드·경고 |
| `grafana/k6:latest` | 약 60MB | 부하 시험 |

> 버전을 `latest`가 아니라 숫자로 고정한 이유가 있습니다. `latest`는 시점에 따라 다른 버전을 받게 되어, 다른 사람과 화면이 달라지거나 설정 파일 형식이 맞지 않을 수 있습니다. Prometheus와 Grafana는 버전을 고정하고, 결과에 영향이 적은 k6만 `latest`로 둡니다.

## 7. Gradle 빌드가 되는지 미리 확인

2절에서 PetClinic을 다시 빌드합니다. 빌드는 처음 실행할 때 의존성을 받느라 몇 분이 걸릴 수 있으므로 미리 한 번 돌려 둡니다.

```bash
# 현재 상태 그대로 한 번 빌드해 본다. 의존성 캐시를 미리 채우는 것이 목적이다.
./gradlew bootJar

# JAR 파일이 만들어졌는지 확인한다.
ls -lh build/libs/*.jar
```

| 확인 | 정상 기준 | 안 되면 |
| --- | --- | --- |
| `./gradlew bootJar` | `BUILD SUCCESSFUL` | Java 17 이상이 설치돼 있는지 `java -version`으로 확인 |
| `build/libs/*.jar` | `spring-petclinic-*.jar` 파일 존재 | 빌드 로그의 오류 메시지 확인 |

이 단계를 미리 해 두면 모임 당일 2절의 빌드가 훨씬 빨리 끝납니다.

## 8. 실습 중 문제가 생겼을 때 되돌리는 방법

6주차 실습은 세 파일을 수정합니다. 문제가 생기면 3절에서 만든 백업으로 되돌립니다.

```bash
# 백업 파일 목록을 확인한다.
ls -l docker-compose.yml.before-week6-* build.gradle.before-week6-*
ls -l src/main/resources/application.properties.before-week6-*

# 5주차 상태로 되돌린다. 파일명의 시각 부분은 위 결과에 맞춰 바꾼다.
cp docker-compose.yml.before-week6-<시각> docker-compose.yml
cp build.gradle.before-week6-<시각> build.gradle
cp src/main/resources/application.properties.before-week6-<시각> src/main/resources/application.properties

# 되돌린 설정으로 5주차 구성을 다시 실행한다.
docker compose up -d
```

`build.gradle`이나 `application.properties`를 되돌렸다면 **이미지를 다시 빌드해야** 반영됩니다.

```bash
./gradlew bootJar && docker build -t spring-petclinic:dockerfile .
docker compose up -d --force-recreate app1 app2
```

DB 볼륨은 어느 경우에도 삭제하지 않습니다.

> `application.properties.before-week6-*` 백업 파일은 `src/main/resources/` 안에 만들어집니다. Spring Boot는 이름이 정확히 `application.properties`인 파일만 읽으므로, 백업 파일이 앱 동작에 영향을 주지 않습니다.

## 완료 기준

- [ ] `docker compose config --services`에 `app1`, `app2`, `nginx`, `postgres` 네 개가 보인다.
- [ ] `default.conf`에 `upstream petclinic_backend`와 `proxy_pass http://petclinic_backend;` 두 줄이 있다.
- [ ] `logs/tomcat/app1/access`, `logs/tomcat/app2/access` 폴더가 있다.
- [ ] 5주차 구성으로 `http://localhost:8080/owners`가 `200`을 반환하는 것을 확인했다.
- [ ] `docker-compose.yml`, `build.gradle`, `application.properties`를 백업했다.
- [ ] 호스트의 `8080`, `9090`, `3000` 포트가 모두 비어 있다.
- [ ] `monitoring/` 아래 다섯 폴더와 `k6/` 폴더를 만들었다.
- [ ] `prom/prometheus`, `grafana/grafana`, `grafana/k6` 이미지를 받았다.
- [ ] `./gradlew bootJar`가 성공하는 것을 확인했다.
- [ ] Prometheus를 `proxy-net`에 붙이는 이유를 설명할 수 있다.

## 참고

- [Prometheus 설치와 설정](https://prometheus.io/docs/prometheus/latest/installation/)
- [Grafana Docker 이미지 설정](https://grafana.com/docs/grafana/latest/setup-grafana/installation/docker/)
- [Grafana k6 설치](https://grafana.com/docs/k6/latest/set-up/install-k6/)
- [Docker Compose 볼륨 설정](https://docs.docker.com/reference/compose-file/services/#volumes)
- [Docker Compose 네트워크 설정](https://docs.docker.com/reference/compose-file/services/#networks)
