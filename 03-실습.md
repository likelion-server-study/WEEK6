# 03. 실습 — Prometheus·Grafana로 장애를 발견하고 복구 확인하기

5주차의 `app1`·`app2`·Nginx·PostgreSQL 구조를 **그대로 두고**, 옆에 관측 도구를 붙인다. 이 문서는 기존 파일을 통째로 덮어쓰지 않고, **바꾸거나 추가할 부분만** 보여 준다.

`01-환경세팅.md`를 먼저 끝낸 상태에서 시작한다. 특히 다음 네 가지가 되어 있어야 한다.

- `docker-compose.yml`, `build.gradle`, `application.properties`를 `*.before-week6-*`로 백업했다.
- `monitoring/` 아래 다섯 폴더와 `k6/` 폴더를 만들었다.
- `prom/prometheus`, `grafana/grafana`, `grafana/k6` 이미지를 받았다.
- 호스트의 `8080`, `9090`, `3000` 포트가 비어 있다.

## 이 문서를 보는 방법

실습에는 다음 화면을 사용한다. 코드 블록 위의 **입력·수정/실행 위치**를 먼저 확인한다.

| 표시 | 실제로 작업할 곳 | 하는 일 |
| --- | --- | --- |
| 터미널 A — 로그 관찰 | **PetClinic 폴더**로 이동한 첫 번째 터미널 | `docker compose logs -f`를 계속 띄워 로그 관찰 |
| 터미널 B — 명령 실행 | 같은 PetClinic 폴더를 연 두 번째 터미널 | `docker`, `curl`, `grep` 같은 명령 입력 |
| 터미널 C — 부하 실행 | 같은 PetClinic 폴더를 연 세 번째 터미널 | 요청을 계속 흘려보내는 용도. **4단계부터 사용** |
| nano 편집 화면 | 터미널 B에서 `nano 파일경로`를 실행하면 열리는 화면 | YAML·Gradle·Properties 수정 |
| PC 브라우저 | Chrome·Edge·Safari 등 | `localhost:8080`, `localhost:9090`, `localhost:3000` 확인 |

5주차와 달리 터미널이 **세 개** 필요하다. 4단계에서 부하를 계속 흘리면서 동시에 장애를 주입해야 하기 때문이다. 3단계까지는 A·B 두 개만 있으면 된다.

Windows 사용자는 WSL2 Ubuntu 터미널을, macOS 사용자는 기본 터미널을 쓴다. 세 터미널 모두 **5주차에서 사용한 PetClinic 폴더**로 이동한다. 이후 별도 안내가 없는 `bash` 명령은 터미널 B에 입력한다.

```bash
# 현재 위치가 PetClinic 폴더인지 파일 목록으로 확인한다.
ls
```

정상이면 `Dockerfile`, `docker-compose.yml`, `build.gradle`, `nginx`, `logs`, 그리고 새로 만든 `monitoring`, `k6`가 보인다.

파일을 수정할 때는 터미널 B에서 안내된 `nano 파일경로`를 실행한다. nano 화면에서 내용을 수정한 뒤 다음 순서로 저장하고 나온다.

```text
Ctrl+O → Enter : 저장
Ctrl+X         : nano 종료
```

> **화면 표시 원칙**
> 각 단계는 `검사 목표 → 입력·수정 → 명령 한눈에 보기 → 결과·로그 판정 → 짧은 설명` 순서로 읽는다. 코드 블록의 주석은 **명령 전체의 목적**을 설명하고, 아래 표는 처음 보면 해석하기 어려운 핵심 구성만 설명한다. `curl -sS`, `grep -Rc`처럼 이전 주차에서 이미 설명한 옵션은 반복하지 않는다.

### 5주차와 달라지는 작업 습관 세 가지

6주차에는 5주차와 다르게 해야 하는 것이 세 가지 있다. 미리 알아 두지 않으면 반드시 막힌다.

| 항목 | 5주차 | 6주차 | 이유 |
| --- | --- | --- | --- |
| 앱 설정을 바꿨을 때 | Compose만 고치면 반영 | **이미지를 다시 빌드**해야 반영 | `build.gradle`·`application.properties`는 JAR 안에 들어간다 |
| 장애를 만드는 방법 | `docker compose stop app1` | **컨테이너 안에서 앱 프로세스를 죽인다** | 밖에서 멈추는 명령(`stop`·`kill`)은 사용자 중지라 자동 재시작이 동작하지 않는다 |
| 앱을 다시 만들 때 | — | **`--force-recreate`** 필요 | 이미지를 바꿔도 기존 컨테이너는 옛 이미지로 계속 돈다 |

첫 줄이 6주차에서 가장 흔한 사고다. `application.properties`를 고치고 `docker compose restart app1`만 하면 **아무것도 바뀌지 않는다.** 반영 경로는 항상 다음과 같다.

```text
파일 수정 → ./gradlew bootJar → docker build → docker compose up -d --force-recreate
```

---

## 시작 전. 개념·이론의 구성 요소를 실습 서비스에 대응하기

이론에서 구분한 개념은 실습 파일에서 다음과 같이 나타난다.

| 이론의 개념 | 실습에서 해당하는 것 |
| --- | --- |
| 앱이 지표를 내놓는다 | `build.gradle`의 `micrometer-registry-prometheus` + `/actuator/prometheus` |
| 엔드포인트 공개 | `application.properties`의 `management.endpoints.web.exposure.include` |
| 히스토그램 버킷 | `application.properties`의 `percentiles-histogram` |
| Pull 방식 수집 | `monitoring/prometheus/prometheus.yml`의 `scrape_configs` |
| 수집 대상 라벨 | `prometheus.yml`의 `labels: app: app1` |
| `up` 지표 | Prometheus가 스스로 기록. `http://localhost:9090`에서 조회 |
| 데이터소스 | `monitoring/grafana/provisioning/datasources/prometheus.yml` |
| 대시보드 | `monitoring/grafana/dashboards/week6-petclinic.json` |
| 경고 규칙 | `monitoring/grafana/provisioning/alerting/alert-rules.yml` |
| 일정한 부하 | 터미널 C의 `while true` + `curl` 반복 (k6는 7-4 선택) |
| 자동 복구 | `docker-compose.yml`의 `restart: unless-stopped` + `init: true` |
| 비정상 종료 | `docker compose exec app1 sh -c 'kill -9 $(pgrep -f "java -jar")'` |

이론 마지막의 "이 구조가 아직 해결하지 못한 것"(먹통 상태 감지, Prometheus 이중화, 로그 수집, 분산 추적)은 이번 주차 범위 밖이므로 실습하지 않는다.

---

## 0. 실습 공통: 이번 주차의 판정 근거 확인 방법

> **이 단계에서 검사하는 것:** 이번 주차 내내 쓸 확인 명령을 미리 훑어 둔다. **지금 실행할 필요는 없다.** 각 명령은 필요한 단계에서 실제로 사용한다.

5주차의 판정 근거 세 가지(`curl` 상태 코드, Nginx 로그, 앱별 Tomcat 로그)는 그대로 쓴다. 여기에 두 가지가 더해진다.

| 근거 | 명령 | 알 수 있는 것 | 쓰는 곳 |
| --- | --- | --- | --- |
| 앱별 Tomcat 로그 | `grep -Rc "study=week6-..." logs/tomcat/app1` | 그 앱까지 도달했는지 | 전 단계 |
| 앱의 지표 엔드포인트 | `docker compose exec nginx wget -qO- http://app1:8080/actuator/prometheus` | 앱이 지표를 내놓고 있는지 | 1-4 |
| Prometheus API | `curl -s --get http://localhost:9090/api/v1/query --data-urlencode 'query=up{job="petclinic"}'` | 시간에 따른 값의 변화 | 2-3 |
| 재시작 횟수 | `docker inspect -f '{{.State.Status}} restarts={{.RestartCount}}' spring-petclinic-app1-1` | Docker가 되살렸는지 | 5-1 |

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `docker compose exec nginx wget -qO-` | **nginx 컨테이너 안에서** 요청을 보낸다. PetClinic 이미지에는 `curl`이 없을 수 있어 nginx의 `wget`을 빌려 쓴다. |
| `http://app1:8080` | Nginx를 거치지 않고 app1에 **직접** 접속한다. Compose 네트워크 안이라 서비스명으로 접근된다. |
| `curl --get --data-urlencode` | 쿼리 문자열을 안전하게 인코딩해 GET으로 보낸다. PromQL에는 `{`, `"`, `=` 같은 문자가 많아 그냥 붙이면 깨진다. |
| `.RestartCount` | 지금까지 다시 시작된 횟수. **사람이 아니라 Docker가 되살렸다는 증거**가 된다. |

**짧은 설명**

5주차의 확인 방법을 버리지 않는다는 점이 중요하다. 새 도구가 알려 주는 숫자를 그대로 믿기 전에, **이미 검증된 방법과 맞춰 보는 것**이 이번 주차의 기본 태도다. Prometheus가 "app1이 5건 처리했다"고 하면 `grep -Rc`로 정말 5건인지 세어 본다.

지금 위 명령을 실행하면 지표 엔드포인트는 404, Prometheus는 연결 실패가 난다. **아직 만들지 않았으니 정상이다.** 1단계부터 하나씩 만든다.

---

## 1. PetClinic이 지표를 내놓게 만들기

### 1-1. 지금은 지표 엔드포인트가 없다는 것 확인

> **이 단계에서 검사하는 것:** 의존성을 추가하기 전에는 `/actuator/prometheus`가 없다는 것을 눈으로 확인한다.

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 5주차 구성을 그대로 띄운다.
docker compose up -d

# 두 앱이 기동됐는지 확인한다.
docker compose ps

# 지표 엔드포인트가 있는지 확인한다. 지금은 없어야 정상이다.
docker compose exec nginx wget -qO- http://app1:8080/actuator/prometheus | head -5

# build.gradle에 관련 의존성이 있는지 확인한다.
grep -n "actuator\|micrometer" build.gradle || echo "관련 의존성 없음"
```

**결과·로그 판정**

| 확인 항목 | 지금의 정상 값 |
| --- | --- |
| `docker compose ps` | `app1`, `app2`, `nginx`, `postgres` 네 개가 `Up` |
| `wget ... /actuator/prometheus` | 아무것도 출력되지 않거나 `404` 관련 메시지 |
| `grep` 결과 | `actuator`만 있거나 `관련 의존성 없음` |

`micrometer-registry-prometheus`가 보이면 이미 추가된 상태이므로 1-2를 건너뛰어도 된다.

**짧은 설명**

`spring-boot-starter-actuator`가 이미 있어도 `/actuator/prometheus`는 생기지 않는다. Actuator는 **창구**일 뿐이고, 그 창구에 Prometheus 형식으로 내보내는 **번역기**가 따로 필요하기 때문이다. 그 번역기가 `micrometer-registry-prometheus`다.

### 1-2. 의존성과 설정 추가

> **이 단계에서 검사하는 것:** 앱이 지표를 만들고 그것을 Prometheus 형식으로 내보내도록 두 파일을 수정한다.

**입력·수정 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 지표 라이브러리를 추가하기 위해 Gradle 빌드 설정을 연다.
nano build.gradle
```

`dependencies { }` 블록 **안에** 다음을 추가한다.

```gradle
    // Actuator: /actuator/** 엔드포인트 제공 (이미 있으면 추가하지 않는다)
    implementation 'org.springframework.boot:spring-boot-starter-actuator'

    // Micrometer Prometheus 레지스트리: /actuator/prometheus 엔드포인트를 만든다
    runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
```

이어서 앱 설정을 연다.

```bash
# 엔드포인트 공개와 히스토그램 설정을 추가하기 위해 앱 설정 파일을 연다.
nano src/main/resources/application.properties
```

파일 **맨 아래에** 다음을 추가한다.

```properties
# --- 6주차: Actuator ---
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true

# --- 6주차: Metrics ---
management.metrics.distribution.percentiles-histogram.http.server.requests=true
management.metrics.distribution.minimum-expected-value.http.server.requests=10ms
management.metrics.distribution.maximum-expected-value.http.server.requests=10s
management.metrics.tags.application=petclinic
```

> **주의 1:** `runtimeOnly`와 `implementation`을 바꿔 써도 동작하지만, 우리 코드가 이 라이브러리를 직접 호출하지 않으므로 `runtimeOnly`가 맞다.
>
> **주의 2:** `percentiles-histogram` 줄을 빠뜨리면 나머지는 전부 정상인데 **p95 응답시간 패널만 영원히 비어 있다.** 이번 주차에서 가장 찾기 어려운 오류이므로 지금 확인해 둔다.
>
> **주의 3:** `management.endpoints.web.exposure.include`에 `shutdown`을 넣지 않는다. HTTP 요청 하나로 앱을 종료시킬 수 있게 되어 위험하다.

**입력·실행 — 터미널 B**

```bash
# 두 파일에 필요한 줄이 실제로 들어갔는지 확인한다.
grep -n "micrometer-registry-prometheus" build.gradle
grep -n "exposure.include" src/main/resources/application.properties
grep -n "percentiles-histogram" src/main/resources/application.properties
```

**결과·로그 판정**

세 명령 모두 줄 번호와 함께 내용이 출력되어야 한다. 아무것도 안 나오면 저장이 안 된 것이다. nano에서 `Ctrl+O` → `Enter` → `Ctrl+X` 순서를 다시 확인한다.

**짧은 설명**

두 파일의 역할이 다르다.

| 파일 | 정하는 것 |
| --- | --- |
| `build.gradle` | 지표를 **만들 수 있는 능력**을 앱에 넣는다 |
| `application.properties` | 그 능력을 **어디까지 공개할지, 무엇을 측정할지** 정한다 |

라이브러리만 넣고 `exposure.include`를 안 쓰면 엔드포인트가 닫혀 있어 404가 난다. 반대로 설정만 쓰고 라이브러리가 없으면 그 이름의 엔드포인트 자체가 존재하지 않는다. 둘 다 필요하다.

### 1-3. 다시 빌드하고 이미지 교체

> **이 단계에서 검사하는 것:** 수정한 설정이 실제로 실행 중인 컨테이너에 반영되는 경로를 확인한다.

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 수정한 build.gradle과 application.properties를 반영해 JAR를 다시 만든다.
./gradlew bootJar

# 새 JAR로 PetClinic 이미지를 다시 만든다. 이미지 이름은 5주차와 같게 유지한다.
docker build -t spring-petclinic:dockerfile .

# 두 앱 컨테이너를 새 이미지로 다시 만든다. restart만 하면 옛 이미지가 그대로 쓰인다.
docker compose up -d --force-recreate app1 app2

# 두 앱이 완전히 기동될 때까지 기다린 뒤 상태를 확인한다.
sleep 10 && docker compose ps
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `./gradlew bootJar` | 소스와 설정을 묶어 실행 가능한 JAR를 만든다. |
| `docker build -t 이름:태그 .` | 현재 폴더의 `Dockerfile`로 이미지를 만든다. 마지막 `.`은 빌드 대상 폴더다. |
| `--force-recreate` | 설정이 안 바뀌었어도 **컨테이너를 강제로 새로 만든다.** 새 이미지를 쓰게 하려면 필요하다. |
| `sleep 30 &&` | 30초 기다린 뒤 다음 명령을 실행한다. Spring Boot 기동에 시간이 걸리기 때문이다. |

**결과·로그 판정**

- `./gradlew bootJar` → `BUILD SUCCESSFUL`
- `docker build` → 마지막 줄에 이미지 이름
- `docker compose ps` → `app1`, `app2`가 다시 `Up`, 실행 시간이 방금으로 초기화됨

**짧은 설명**

`--force-recreate`가 필요한 이유가 이번 단계의 핵심이다.

```text
docker build          → 새 이미지가 만들어진다
docker compose restart → 컨테이너를 껐다 켤 뿐. 옛 이미지 그대로.
docker compose up -d --force-recreate → 새 이미지로 컨테이너를 다시 만든다
```

이미지는 **틀**이고 컨테이너는 그 틀로 찍어 낸 **물건**이다. 틀을 바꿔도 이미 찍어 낸 물건은 그대로다. 6주차에서 "설정을 고쳤는데 반영이 안 된다"는 문제의 대부분이 이 차이에서 나온다.

### 1-4. 지표 엔드포인트가 생겼는지 확인

> **이 단계에서 검사하는 것:** 두 앱 모두 `/actuator/prometheus`를 제공하고, 필요한 지표가 실제로 들어 있는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 지표가 만들어지도록 요청을 먼저 몇 건 보낸다. 요청이 없으면 요청 수 지표도 없다.
for i in $(seq 1 5); do
  curl -s -o /dev/null "http://localhost:8080/owners?study=week6-warmup-$i"
done

# app1의 지표 엔드포인트가 응답하는지 확인한다.
docker compose exec nginx wget -qO- http://app1:8080/actuator/prometheus | head -10

# app2도 같은지 확인한다.
docker compose exec nginx wget -qO- http://app2:8080/actuator/prometheus | head -3

# 필요한 지표가 실제로 들어 있는지 한 번에 센다.
docker compose exec nginx wget -qO- http://app1:8080/actuator/prometheus \
  | grep -cE "^http_server_requests_seconds_count|^http_server_requests_seconds_bucket|^jvm_memory_used_bytes|^hikaricp_connections_active"

# p95 계산에 꼭 필요한 히스토그램 버킷만 따로 센다.
docker compose exec nginx wget -qO- http://app1:8080/actuator/prometheus \
  | grep -c "^http_server_requests_seconds_bucket"
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `grep -c "^http_server..."` | 줄 **맨 앞**이 그 이름인 줄의 개수를 센다. `^`가 없으면 주석(`# HELP ...`)까지 세어 개수가 부풀려진다. |
| `grep -cE "A\|B\|C"` | 여러 이름 중 하나라도 맞는 줄을 모두 센다. |

**결과·로그 판정**

| 확인 항목 | 정상 값 | 0이면 |
| --- | --- | --- |
| `head -10` 출력 | `# HELP`, `# TYPE`으로 시작하는 텍스트 | 1-3의 빌드·재생성을 다시 확인 |
| 지표 합계 개수 | `10` 이상 | 앱이 아직 기동 중일 수 있다. 30초 뒤 다시 확인 |
| `..._bucket` 개수 | **`1` 이상 (보통 10 이상)** | `percentiles-histogram` 설정 누락. 1-2로 돌아간다 |

`_bucket`만 `0`인 경우가 특히 중요하다. 나머지 지표는 다 나오는데 이것만 없으면 **설정이 빠진 것**이고, 4단계의 p95 패널이 통째로 비게 된다.

**짧은 설명**

이 출력이 이번 주차의 출발점이다. 이건 **특별한 프로토콜이 아니라 그냥 텍스트 문서**다. 각 줄은 `지표이름{라벨="값",...} 숫자` 형태이고, `# HELP`와 `# TYPE`은 사람이 읽기 위한 주석이다.

```text
# HELP http_server_requests_seconds
# TYPE http_server_requests_seconds histogram
http_server_requests_seconds_count{method="GET",status="200",uri="/owners"} 5.0
http_server_requests_seconds_sum{method="GET",status="200",uri="/owners"} 0.412
```

Prometheus가 하는 일은 이 페이지를 5초마다 읽어서 시각과 함께 저장하는 것뿐이다. 지금은 앱이 페이지를 열어 놓았을 뿐, 아무도 읽어 가지 않는 상태다. 읽어 갈 사람을 2단계에서 만든다.

`uri`가 `/owners`처럼 **경로 패턴**으로 묶이는 점도 확인해 둔다. `/owners/1`, `/owners/2`가 각각 따로 잡히면 시계열이 무한히 늘어난다.

---

## 2. Prometheus를 추가하고 두 앱을 각각 수집

### 2-1. 수집 설정 파일 만들기

> **이 단계에서 검사하는 것:** Prometheus가 무엇을 얼마나 자주 읽어 갈지 정의한다.

**입력·수정 — 터미널 B · 위치: PetClinic 폴더**

```bash
# Prometheus 수집 설정 파일을 새로 만든다.
nano monitoring/prometheus/prometheus.yml
```

nano 화면에 다음 내용을 그대로 입력한다.

```yaml
global:
  scrape_interval: 5s
  scrape_timeout: 3s
  evaluation_interval: 5s

scrape_configs:
  - job_name: petclinic
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["app1:8080"]
        labels:
          app: app1
      - targets: ["app2:8080"]
        labels:
          app: app2

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
```

**입력·실행 — 터미널 B**

```bash
# 파일이 만들어졌고 두 앱이 각각 대상으로 들어갔는지 확인한다.
grep -n -E "job_name|targets|app:" monitoring/prometheus/prometheus.yml

# YAML 들여쓰기와 설정 내용을 Prometheus 자신에게 검사시킨다.
docker run --rm --entrypoint promtool \
  -v "$PWD/monitoring/prometheus/prometheus.yml:/p.yml:ro" \
  prom/prometheus:v3.1.0 check config /p.yml
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `scrape_interval: 5s` | 5초마다 대상을 읽어 간다. 실습에서 장애를 빨리 보기 위한 값이다. |
| `metrics_path` | 읽어 갈 주소의 경로. Spring Boot는 `/actuator/prometheus`. |
| `targets: ["app1:8080"]` | 읽어 갈 대상. **Nginx가 아니라 앱에 직접** 접속한다. |
| `labels: app: app1` | 이 대상에서 온 모든 지표에 `app="app1"` 라벨을 붙인다. |
| `promtool check config` | Prometheus에 들어 있는 검사 도구. 들여쓰기뿐 아니라 **설정 항목 이름까지** 검사한다. |

**결과·로그 판정**

- `job_name`이 `petclinic`, `prometheus` 두 개
- `targets`가 `app1:8080`, `app2:8080`, `localhost:9090` 세 줄
- `app: app1`, `app: app2` 두 줄
- `promtool` 결과가 `SUCCESS: /p.yml is valid prometheus config file syntax`

`FAILED`가 나오면 오류 줄 번호가 함께 표시된다. YAML은 탭이 아니라 **공백**만 쓴다.

**짧은 설명**

`targets`에 `nginx:80`을 쓰지 않는 이유가 이 단계의 핵심이다.

```text
Prometheus → nginx:80 → app1 또는 app2 (라운드 로빈)
  → 이번 수집이 어느 앱의 값인지 알 수 없다
  → 두 앱의 지표가 한 시계열에 뒤섞인다

Prometheus → app1:8080  (직접)
Prometheus → app2:8080  (직접)
  → 각각 따로 저장된다
```

5주차에서 만든 로드밸런싱을 **일부러 우회하는 것**이다. 사용자 요청은 분산되어야 하지만, 관측은 분산되면 안 된다.

`app` 라벨을 앱 쪽이 아니라 Prometheus 쪽에서 붙이는 이유도 있다. 앱 설정으로 붙이면 이름을 바꿀 때마다 **이미지를 다시 빌드**해야 한다. Prometheus 설정에 두면 파일 한 줄만 고치면 된다.

### 2-2. Compose에 Prometheus 추가

> **이 단계에서 검사하는 것:** Prometheus 컨테이너를 `proxy-net`에 붙여 앱 이름을 해석할 수 있게 한다.

**입력·수정 — 터미널 B**

```bash
# Prometheus 서비스를 추가하기 위해 Compose 파일을 연다.
nano docker-compose.yml
```

**① `services:` 블록 맨 아래(`nginx` 서비스 다음, `networks:` 앞)에 추가한다**

```yaml
  prometheus:
    image: prom/prometheus:v3.1.0
    container_name: week6-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=6h
      - --web.enable-lifecycle
    networks:
      - proxy-net
```

**② 파일 맨 아래 `volumes:` 블록에 한 줄 추가한다**

```yaml
volumes:
  # ... 5주차의 postgres 볼륨은 그대로 ...
  prometheus-data:
```

> **주의 1:** `networks: - proxy-net`을 빠뜨리면 Prometheus가 `app1`이라는 이름을 찾지 못한다. 5주차에서 Nginx가 `upstream`의 이름을 해석하지 못하던 것과 같은 문제다.
>
> **주의 2:** `volumes:` 블록에 `prometheus-data:`를 선언하지 않으면 Compose가 "정의되지 않은 볼륨"이라며 시작하지 못한다. 서비스 안의 `prometheus-data:/prometheus`와 파일 맨 아래의 선언은 **둘 다** 필요하다.
>
> **주의 3:** 호스트의 9090 포트가 이미 쓰이고 있으면 `"9091:9090"`처럼 **왼쪽 숫자만** 바꾼다.

**입력·실행 — 터미널 B**

```bash
# 합쳐진 최종 Compose 설정에 문법 오류가 없는지 확인한다.
docker compose config >/dev/null && echo "Compose 문법 정상"

# prometheus 서비스가 추가됐는지 확인한다.
docker compose config --services

# Prometheus만 먼저 실행한다.
docker compose up -d prometheus

# 기동 로그에 오류가 없는지 확인한다.
docker compose logs --tail 20 prometheus
```

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| `Compose 문법 정상` | 출력됨 |
| `--services` | `app1`, `app2`, `nginx`, `postgres`, `prometheus` 다섯 개 |
| `logs prometheus` | `Server is ready to receive web requests.` |

로그에 `error loading config`가 나오면 `prometheus.yml`의 들여쓰기 문제다. 2-1로 돌아간다.

**짧은 설명**

`:ro`로 설정 파일을 연결한 이유는 컨테이너가 이 파일을 고칠 이유가 없기 때문이다. 실수로 덮어쓰는 것을 막는다.

반면 지표 데이터는 호스트 폴더가 아니라 **Named Volume**(`prometheus-data`)에 저장한다. 호스트 폴더로 연결하면 컨테이너 안의 사용자 권한과 호스트 권한이 맞지 않아 시작에 실패하는 경우가 흔하다. 5주차에서 Tomcat 로그를 호스트 폴더에 연결한 것과 목적이 다르다. 로그는 우리가 `grep`으로 읽어야 하지만, Prometheus 데이터는 Prometheus만 읽으면 된다.

### 2-3. 수집이 되는지 확인

> **이 단계에서 검사하는 것:** Prometheus가 app1·app2를 각각 정상적으로 읽어 가고 있는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# Prometheus가 준비됐는지 확인한다.
curl -s http://localhost:9090/-/ready

# 수집 대상의 상태를 확인한다. 두 앱이 모두 up이어야 한다.
curl -s http://localhost:9090/api/v1/targets \
  | grep -o '"health":"[a-z]*"' | sort | uniq -c

# up 지표를 직접   조회한다.
curl -s --get http://localhost:9090/api/v1/query \
  --data-urlencode 'query=up{job="petclinic"}'

# 요청을 몇 건 보내 지표를 만든다.
for i in $(seq 1 10); do
  curl -s -o /dev/null "http://localhost:8080/owners?study=week6-scrape-$i"
done

# 잠시 기다린 뒤 앱별 요청 처리량을 조회한다.
sleep 15
curl -s --get http://localhost:9090/api/v1/query \
  --data-urlencode 'query=sum by (app) (rate(http_server_requests_seconds_count{job="petclinic"}[1m]))'
```

**브라우저 확인**

`http://localhost:9090/targets` 를 연다.

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `/-/ready` | Prometheus가 요청을 받을 준비가 됐는지 확인하는 전용 주소. |
| `/api/v1/targets` | 수집 대상 목록과 각 대상의 상태를 JSON으로 준다. |
| `grep -o '"health":"[a-z]*"' \| sort \| uniq -c` | JSON에서 상태 값만 뽑아 **개수별로 센다.** 대상이 많을 때 유용하다. |
| `sleep 15` | `rate(...[1m])`는 최소 두 번 이상 수집돼야 값이 나온다. 기다렸다 조회한다. |

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| `/-/ready` | `Prometheus Server is Ready.` |
| health 개수 | `3 "health":"up"` (petclinic 2개 + prometheus 자신 1개) |
| `up{job="petclinic"}` 조회 | 결과가 **두 개**, 둘 다 `"value":[...,"1"]` |
| 브라우저 `/targets` | `petclinic` 잡 아래 `app1:8080`, `app2:8080`이 초록색 **UP** |
| 요청 처리량 조회 | `app="app1"`, `app="app2"` 두 결과에 0보다 큰 값 |

`up`이 `0`이거나 targets가 빨간색이면 원인은 대개 셋 중 하나다.

| 원인 | 확인 방법 |
| --- | --- |
| Prometheus가 `proxy-net`에 없다 | `docker inspect week6-prometheus \| grep -A5 Networks` |
| 앱에 `/actuator/prometheus`가 없다 | 1-4를 다시 실행 |
| 앱이 아직 기동 중이다 | `docker compose logs --tail 5 app1` |

**짧은 설명**

`up` 지표가 **두 개** 나오는 것이 이번 단계의 핵심이다.

```json
{"metric":{"__name__":"up","app":"app1","instance":"app1:8080","job":"petclinic"},"value":[...,"1"]}
{"metric":{"__name__":"up","app":"app2","instance":"app2:8080","job":"petclinic"},"value":[...,"1"]}
```

`app` 라벨로 구분되어 있으므로, 앞으로 `up{app="app1"}`처럼 한쪽만 골라 볼 수 있다. 이 값이 `1`에서 `0`으로 바뀌는 것이 5단계에서 관찰할 장애의 신호다.

`up`은 앱이 만든 지표가 아니라 **Prometheus가 수집 성공/실패를 스스로 기록한 값**이라는 점을 다시 확인해 둔다. 그래서 앱이 완전히 죽어 아무 응답도 못 하는 상황에서도 `0`이라는 기록이 남는다.

---

## 3. Grafana를 추가하고 대시보드·경고 확인

### 3-1. 설정 파일 세 개 만들기

> **이 단계에서 검사하는 것:** Grafana가 시작할 때 데이터소스·대시보드·경고를 자동으로 읽어 가도록 파일을 배치한다.

Grafana 설정은 파일 네 개로 나뉜다. 역할을 먼저 정리한다.

| 파일 | 정하는 것 |
| --- | --- |
| `provisioning/datasources/prometheus.yml` | 어느 Prometheus에 질의할지 |
| `provisioning/dashboards/dashboards.yml` | 대시보드 파일을 어느 폴더에서 읽을지 (**지시서**) |
| `dashboards/week6-petclinic.json` | 실제 패널과 쿼리 (**내용**) |
| `provisioning/alerting/alert-rules.yml` | 경고 규칙 |

**입력·수정 — 터미널 B · 위치: PetClinic 폴더**

```bash
# ① 데이터소스
nano monitoring/grafana/provisioning/datasources/prometheus.yml
```

```yaml
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: prometheus
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      httpMethod: POST
      timeInterval: 5s
```

```bash
# ② 대시보드 지시서
nano monitoring/grafana/provisioning/dashboards/dashboards.yml
```

```yaml
apiVersion: 1

providers:
  - name: week6
    orgId: 1
    folder: Week6
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
```

**③ 대시보드 내용**은 JSON 약 290줄이라 손으로 입력하지 않는다. 아래 명령 한 줄로 내려받는다.

```bash
# 대시보드 JSON을 저장소에서 바로 내려받는다.
curl -fsSL -o monitoring/grafana/dashboards/week6-petclinic.json \
  https://raw.githubusercontent.com/likelion-server-study/WEEK6/main/files/monitoring/grafana/dashboards/week6-petclinic.json

# 내려받아졌는지, JSON이 깨지지 않았는지 확인한다.
ls -l monitoring/grafana/dashboards/week6-petclinic.json
grep -c '"title"' monitoring/grafana/dashboards/week6-petclinic.json
```

`grep -c` 결과가 `8`이면 정상이다. (대시보드 제목 1개 + 패널 제목 7개)

**대안 — `curl`이 `429`로 실패하는 경우**

```text
curl: (56) The requested URL returned error: 429
```

`429`는 **GitHub이 요청을 일시적으로 거절**했다는 뜻이다. 파일이 없거나 주소가 틀린 것이 아니다. 같은 공용 IP를 쓰는 여러 명이 동시에 내려받으면(강의실·회사 네트워크) 잘 발생한다. `curl -f`는 실패하면 파일을 만들지 않으므로, 이어지는 `ls`도 `No such file`이 난다.

아래 **대안 두 가지 중 하나**를 쓴다.

```bash
# 대안 1: 저장소를 통째로 받아서 복사한다. (권장)
git clone --depth 1 https://github.com/likelion-server-study/WEEK6.git /tmp/week6
cp /tmp/week6/files/monitoring/grafana/dashboards/week6-petclinic.json \
   monitoring/grafana/dashboards/

# 대안 2: 잠시 기다렸다가 curl을 다시 시도한다. (보통 몇 분이면 풀린다)
sleep 180
curl -fsSL -o monitoring/grafana/dashboards/week6-petclinic.json \
  https://raw.githubusercontent.com/likelion-server-study/WEEK6/main/files/monitoring/grafana/dashboards/week6-petclinic.json
```

> **대안 3 (위 둘 다 막혔을 때):** 브라우저에서 [week6-petclinic.json](https://github.com/likelion-server-study/WEEK6/blob/main/files/monitoring/grafana/dashboards/week6-petclinic.json)을 열어 **Raw** 버튼을 누른 뒤 전체를 복사해 `nano`로 붙여 넣는다. 다만 JSON은 중괄호 하나만 빠져도 Grafana가 대시보드를 **통째로 무시**하므로 마지막 수단으로만 쓴다.

어느 방법을 쓰든 위의 `grep -c '"title"'`가 `8`이 나오는지 확인하고 넘어간다.

**④ 경고 규칙**은 3-4에서 내용을 직접 읽으며 설명하므로, 손으로 입력한다.

```bash
nano monitoring/grafana/provisioning/alerting/alert-rules.yml
```

```yaml
apiVersion: 1

groups:
  - orgId: 1
    name: week6-petclinic
    folder: Week6
    interval: 10s
    rules:
      # 규칙 ①: 앱이 죽으면 알린다.
      - uid: week6-app-down
        title: PetClinic app DOWN
        condition: C
        for: 20s
        annotations:
          summary: "PetClinic 앱이 응답하지 않습니다"
        labels:
          severity: critical
        isPaused: false
        noDataState: Alerting
        execErrState: Alerting
        data:
          - refId: A                      # ① 쿼리: 시계열을 가져온다
            relativeTimeRange: { from: 300, to: 0 }
            datasourceUid: prometheus
            model:
              refId: A
              editorMode: code
              expr: up{job="petclinic"}
              instant: false
              range: true
          - refId: B                      # ② 축약: 마지막 값 하나로 줄인다
            relativeTimeRange: { from: 300, to: 0 }
            datasourceUid: __expr__
            model:
              refId: B
              type: reduce
              expression: A
              reducer: last
              settings: { mode: dropNN }
          - refId: C                      # ③ 비교: 1 미만이면 참
            relativeTimeRange: { from: 300, to: 0 }
            datasourceUid: __expr__
            model:
              refId: C
              type: threshold
              expression: B
              conditions:
                - evaluator: { type: lt, params: [1] }

      # 규칙 ②: 5xx 오류율이 5%를 넘으면 알린다. (7-3에서 확인한다)
      - uid: week6-5xx-rate
        title: PetClinic 5xx 오류율 증가
        condition: C
        for: 20s
        annotations:
          summary: "5xx 오류율이 5%를 넘었습니다"
        labels:
          severity: warning
        isPaused: false
        noDataState: OK
        execErrState: Alerting
        data:
          - refId: A
            relativeTimeRange: { from: 300, to: 0 }
            datasourceUid: prometheus
            model:
              refId: A
              editorMode: code
              # `or vector(0)`이 없으면 5xx가 한 건도 없을 때 결과가 비어 NoData가 된다.
              expr: 100 * (sum(rate(http_server_requests_seconds_count{job="petclinic", uri!="/actuator/prometheus", status=~"5.."}[1m])) or vector(0)) / clamp_min(sum(rate(http_server_requests_seconds_count{job="petclinic", uri!="/actuator/prometheus"}[1m])), 0.001)
              instant: false
              range: true
          - refId: B
            relativeTimeRange: { from: 300, to: 0 }
            datasourceUid: __expr__
            model:
              refId: B
              type: reduce
              expression: A
              reducer: last
              settings: { mode: dropNN }
          - refId: C
            relativeTimeRange: { from: 300, to: 0 }
            datasourceUid: __expr__
            model:
              refId: C
              type: threshold
              expression: B
              conditions:
                - evaluator: { type: gt, params: [5] }
```

```bash
# 네 파일이 모두 제자리에 있는지 확인한다.
find monitoring -type f | sort
```

> `expr:` 줄은 **한 줄로 이어져야 한다.** 복사할 때 줄바꿈이 끼어들면 Grafana가 규칙을 읽지 못한다.

**결과·로그 판정**

```text
monitoring/grafana/dashboards/week6-petclinic.json
monitoring/grafana/provisioning/alerting/alert-rules.yml
monitoring/grafana/provisioning/dashboards/dashboards.yml
monitoring/grafana/provisioning/datasources/prometheus.yml
monitoring/prometheus/prometheus.yml
```

다섯 줄이 모두 나와야 한다.

**짧은 설명**

`uid: prometheus`가 중요하다. 대시보드 JSON과 경고 규칙이 **이 식별자로 데이터소스를 참조**한다.

```text
datasources/prometheus.yml   uid: prometheus          ← 이름표를 붙이고
dashboards/*.json            "uid": "prometheus"      ← 이름표로 찾고
alerting/alert-rules.yml     datasourceUid: prometheus ← 이름표로 찾는다
```

`uid`를 적지 않으면 Grafana가 임의의 값을 만들어 붙인다. 그러면 대시보드가 "그런 데이터소스 없음" 오류를 낸다. 이번 주차에서 대시보드가 비어 보이는 두 번째로 흔한 원인이다.

`provisioning/dashboards/`(지시서)와 `dashboards/`(내용)가 나뉜 구조도 확인해 둔다. 지시서는 "이 폴더를 봐라"라고만 말하고, 폴더 안에 JSON을 몇 개 넣든 Grafana가 알아서 다 읽는다.

### 3-2. Compose에 Grafana 추가

> **이 단계에서 검사하는 것:** Grafana 컨테이너를 띄우고 설정 파일이 제대로 읽혔는지 확인한다.

**입력·수정 — 터미널 B**

```bash
# Grafana 서비스를 추가하기 위해 Compose 파일을 연다.
nano docker-compose.yml
```

**① `prometheus` 서비스 다음에 추가한다**

```yaml
  grafana:
    image: grafana/grafana:11.5.1
    container_name: week6-grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_DEFAULT_THEME: light
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - proxy-net
```

**② `volumes:` 블록에 한 줄 추가한다**

```yaml
volumes:
  # ... postgres, prometheus-data ...
  grafana-data:
```

**입력·실행 — 터미널 B**

```bash
# 문법을 확인하고 Grafana를 실행한다.
docker compose config >/dev/null && docker compose up -d grafana

# 기동될 때까지 기다린다.
sleep 20

# Grafana가 살아 있는지 확인한다.
# 응답 끝에 줄바꿈이 없어 프롬프트가 결과를 덮으므로 `; echo`를 붙인다.
curl -s http://localhost:3000/api/health; echo

# 프로비저닝 관련 로그에 오류가 없는지 확인한다.
docker compose logs grafana | grep -iE "provision|error|alert" | tail -20
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `/api/health` | Grafana의 상태를 JSON으로 알려 주는 전용 주소. |
| `grep -iE "provision\|error\|alert"` | 대소문자 구분 없이 세 단어 중 하나가 있는 줄만 본다. Grafana 로그는 길어서 좁혀 봐야 한다. |
| `depends_on: - prometheus` | Prometheus를 먼저 시작한다. 다만 **기동 완료까지 기다리지는 않는다.** |
| `:ro` (provisioning) | Grafana가 설정 파일을 고치지 못하게 한다. |

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| `/api/health` | `"database": "ok"`가 포함된 JSON |
| 로그 | `inserting datasource from configuration ... uid=prometheus` |
| 로그 | `finished to provision alerting` |
| 로그 | `finished to provision dashboards` |

세 줄이 모두 보이면 설정 파일 네 개가 정상적으로 읽힌 것이다.

> **무시해도 되는 오류 한 줄**
>
> ```text
> level=error msg="Failed to read plugin provisioning files from directory"
> path=/etc/grafana/provisioning/plugins error="no such file or directory"
> ```
>
> 우리는 플러그인을 쓰지 않아 `provisioning/plugins` 폴더를 만들지 않았다. Grafana가 폴더 네 종류를 모두 확인하면서 남기는 것이라 **실습에 영향이 없다.**

로그에 `failed to provision alerting`이 나오면 `alert-rules.yml`의 형식 문제다. 오류 줄에 표시된 항목을 확인한다.

**짧은 설명**

`depends_on`은 **시작 순서만** 정하고 준비 완료를 기다리지 않는다. 그래서 Grafana가 Prometheus보다 먼저 준비될 수 있는데, 문제가 되지 않는다. Grafana는 화면을 그릴 때마다 Prometheus에 질의하므로, 그때 살아 있으면 된다.

`grafana-data` 볼륨과 `dashboards` 볼륨이 둘 다 있는 것이 헷갈릴 수 있다. 전자는 Grafana의 내부 DB(사용자, 설정 등)이고, 후자는 우리가 넣어 준 대시보드 파일이다.

### 3-3. 대시보드 확인

> **이 단계에서 검사하는 것:** 일곱 패널에 값이 그려지는지, 특히 p95 패널이 비어 있지 않은지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 그래프에 그릴 데이터가 생기도록 요청을 30건 보낸다.
for i in $(seq 1 30); do
  curl -s -o /dev/null "http://localhost:8080/owners?study=week6-dash-$i"
  sleep 0.3
done

# 앱별로 나뉘어 처리됐는지 5주차 방식으로 확인한다.
grep -Rc "study=week6-dash-" logs/tomcat/app1
grep -Rc "study=week6-dash-" logs/tomcat/app2
```

**브라우저 확인**

`http://localhost:3000` 을 열고 `admin` / `admin`으로 로그인한다. (비밀번호 변경 화면은 `Skip`)

왼쪽 메뉴 → **Dashboards** → **Week6** 폴더 → **Week6 · PetClinic 장애 관측**

**결과·로그 판정**

| 패널 | 지금 보여야 하는 것 |
| --- | --- |
| app 상태 | `app1` UP, `app2` UP (초록색) |
| 초당 요청 수 | 두 앱 모두 0보다 큰 선 |
| HTTP 오류율 | `0` 근처의 평평한 선 |
| 응답시간 p95 / p50 | **선이 그려진다** (비어 있으면 안 된다) |
| JVM Heap 사용량 | 두 앱의 used/max 선 |
| JVM 라이브 스레드 수 | 두 앱의 선 |
| DB 커넥션 풀 | active/idle 선 |

`grep -Rc` 결과는 두 앱의 합이 `30`이고 양쪽 모두 0이 아니어야 한다.

| 증상 | 원인 | 대처 |
| --- | --- | --- |
| 모든 패널이 비어 있음 | 데이터소스 uid 불일치 | 3-1의 `uid: prometheus` 확인 |
| p95 패널만 비어 있음 | 히스토그램 버킷 없음 | 1-2의 `percentiles-histogram` 확인 후 **다시 빌드** |
| 대시보드 자체가 없음 | 지시서의 `path` 오타 | `dashboards.yml`의 `path`와 Compose 볼륨 경로가 같은지 확인 |
| 값이 계단처럼 끊김 | 요청이 드문드문 들어감 | 정상. 4단계에서 부하를 계속 흘리면 매끄러워진다 |

**짧은 설명**

패널 하나를 열어 보면 Grafana가 무엇을 하는지 바로 보인다. **초당 요청 수** 패널 제목 → **Edit**을 누르면 아래에 쿼리가 그대로 있다.

```promql
sum by (app) (rate(http_server_requests_seconds_count{job="petclinic", uri!="/actuator/prometheus"}[1m]))
```

2-3에서 `curl`로 직접 던졌던 것과 같은 쿼리다. **Grafana는 이 쿼리를 대신 던져 주고 결과를 선으로 그려 줄 뿐**이다. 자기 데이터는 하나도 갖고 있지 않다.

`uri!="/actuator/prometheus"` 조건이 붙은 이유도 확인해 둔다. Prometheus가 5초마다 지표를 읽어 가는 것도 앱 입장에서는 HTTP 요청이라 지표에 잡힌다. 이걸 빼지 않으면 사용자 요청이 0건이어도 초당 0.2건이 계속 잡혀 그래프가 헷갈린다.

### 3-4. 경고 규칙이 등록됐는지 확인

> **이 단계에서 검사하는 것:** 장애를 만들기 전에, 경고 규칙이 Normal 상태로 등록돼 있는지 먼저 확인한다.

**브라우저 확인**

왼쪽 메뉴 → **Alerting** → **Alert rules**

**입력·실행 — 터미널 B**

```bash
# 등록된 경고 규칙의 이름과 현재 상태를 확인한다.
curl -s -u admin:admin http://localhost:3000/api/v1/provisioning/alert-rules \
  | grep -o '"title":"[^"]*"'

# 경고의 현재 상태를 확인한다. 지금은 아무것도 발생하지 않아야 정상이다.
curl -s -u admin:admin http://localhost:3000/api/alertmanager/grafana/api/v2/alerts \
  | head -c 200
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `-u admin:admin` | Grafana API에 아이디·비밀번호로 인증한다. |
| `/api/v1/provisioning/alert-rules` | 파일로 등록된 경고 규칙 목록을 준다. |
| `head -c 200` | 앞 200글자만 본다. JSON이 길게 쏟아지는 것을 막는다. |

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| 규칙 목록 | `PetClinic app DOWN`, `PetClinic 5xx 오류율 증가` 두 개 |
| 브라우저 Alert rules | 두 규칙이 **Normal**(초록색) |
| 발생 중인 경고 | `[]` (빈 배열) |

규칙이 하나도 없으면 `alert-rules.yml`이 읽히지 않은 것이다. 3-2의 로그 확인으로 돌아간다.

**짧은 설명**

경고 규칙의 구조를 한 번 짚어 둔다. `alert-rules.yml`을 보면 규칙 하나가 `data` 아래 세 덩어리(`refId: A`, `B`, `C`)로 되어 있다.

```text
A: 쿼리          up{job="petclinic"}   → 시계열 (여러 시점의 값)
B: 축약 reduce   last                  → 마지막 값 하나로
C: 비교 threshold  < 1                 → 참/거짓
condition: C     → C의 결과로 판정한다
```

쿼리 결과는 시점별 값의 나열이라 그대로는 참/거짓을 말할 수 없다. 그래서 값 하나로 줄인 뒤 임계값과 비교한다. 세 단계가 필요한 이유다.

`for: 20s`는 조건이 참이 된 뒤 **20초를 더 기다렸다가** Alerting으로 바꾼다는 뜻이다. 그 사이는 `Pending`이다. 수집이 한 번 튀었다고 바로 알림이 울리는 것을 막기 위한 장치다.

---

## 4. `app1`을 비정상 종료하고 장애를 관측

### 4-1. 자동 재시작 정책 적용

> **이 단계에서 검사하는 것:** 두 앱에 재시작 정책과 `init`을 넣고, 둘 다 실제로 적용됐는지 확인한다.

**입력·수정 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 재시작 정책을 추가하기 위해 Compose 파일을 연다.
nano docker-compose.yml
```

`app1`과 `app2` 각각에 **두 줄씩** 추가한다.

```yaml
  app1:
    # ... 기존 내용 그대로 ...
    restart: unless-stopped     # ← 추가
    init: true                  # ← 추가

  app2:
    # ... 기존 내용 그대로 ...
    restart: unless-stopped     # ← 추가
    init: true                  # ← 추가
```

`init: true`가 왜 필요한지는 이론 7-3에 있다. 짧게 말하면, 이 줄이 없으면 JVM이 PID 1이 되고 **PID 1은 컨테이너 안에서 보낸 `kill -9`를 무시한다.** 그러면 4-2에서 앱을 죽여도 아무 일이 일어나지 않는다.

**입력·실행 — 터미널 B**

```bash
# 두 서비스에 두 줄이 모두 들어갔는지 확인한다.
grep -n "restart:\|init:" docker-compose.yml

# 설정을 반영하려면 컨테이너를 다시 만들어야 한다.
docker compose up -d --force-recreate app1 app2

# 기동을 기다린다.
sleep 30

# 실제로 컨테이너에 적용됐는지 확인한다. 파일이 아니라 컨테이너를 본다.
for s in app1 app2; do
  echo -n "$s : "
  echo -n "policy=$(docker inspect -f '{{.HostConfig.RestartPolicy.Name}}' $(docker compose ps -q $s)) "
  echo "pid1=$(docker compose exec -T $s cat /proc/1/comm)"
done
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `restart: unless-stopped` | 앱이 죽으면 다시 실행한다. 단 사용자가 `stop`·`kill`로 멈춘 것은 제외한다. |
| `init: true` | `docker-init`을 PID 1로 띄우고 JVM을 그 자식으로 만든다. 그래야 안에서 죽일 수 있다. |
| `--force-recreate` | 두 설정 모두 **컨테이너를 만들 때** 정해진다. 기존 컨테이너에는 반영되지 않는다. |
| `/proc/1/comm` | 컨테이너 안에서 PID 1인 프로세스의 이름. `init`이 적용됐는지 보는 확실한 방법. |

**결과·로그 판정**

```text
app1 : policy=unless-stopped pid1=docker-init
app2 : policy=unless-stopped pid1=docker-init
```

`policy=no`가 나오면 `--force-recreate`를 하지 않은 것이다. `pid1=java`가 나오면 `init: true`가 적용되지 않은 것이다. 둘 중 하나라도 다르면 Compose 파일을 다시 확인하고 재생성한다.

**짧은 설명**

파일이 아니라 **컨테이너를 확인하는 것**이 중요하다. Compose 파일에 써도 기존 컨테이너에는 반영되지 않는다. 재시작 정책과 `init` 모두 컨테이너를 생성하는 시점에 Docker 데몬에 등록되기 때문이다.

이 확인을 건너뛰면 4-2에서 앱을 죽여도 되살아나지 않고, 원인을 찾느라 시간을 쓰게 된다. 두 값이 각각 다른 증상을 만든다.

| 잘못된 값 | 4-2에서 나타나는 증상 |
| --- | --- |
| `policy=no` | 앱은 죽지만 되살아나지 않는다 |
| `pid1=java` | **앱이 아예 죽지 않는다.** 명령을 실행해도 아무 일이 없다 |

### 4-2. 부하를 흘리면서 `app1` 강제 종료

> **이 단계에서 검사하는 것:** 사용자 요청이 계속 들어오는 상태에서 앱 하나가 죽었을 때 무슨 일이 일어나는지 전 과정을 관찰한다.

터미널 C에서 부하를 흘리는 동안, 터미널 B에서 app1을 죽이고 결과를 확인한다.

**① 입력·실행 — 터미널 C (부하)**

```bash
# 0.2초마다 요청을 하나씩 계속 보낸다. 이 터미널은 그대로 둔다.
# 끝낼 때는 Ctrl+C 를 누른다.
while true; do
  curl -s -o /dev/null -w "%{http_code} " "http://localhost:8080/owners?study=week6-fail"
  sleep 0.2
done
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `while true; do ... done` | 조건이 항상 참이라 **Ctrl+C를 누를 때까지** 반복한다. |
| `sleep 0.2` | 요청 사이에 0.2초를 쉰다. 초당 약 5건이 흐른다. |
| `-w "%{http_code} "` | 상태 코드만 이어서 찍는다. **`200`이 계속 흐르다 끊기는지**를 눈으로 본다. |

`200 200 200 ...`이 한 줄로 계속 이어지면 정상이다. 이 터미널은 실습이 끝날 때까지 그대로 둔다.

**② 입력·실행 — 터미널 B (장애 주입) · 부하가 시작되고 30초쯤 뒤**

```bash
# 몇 시에 죽였는지 기록해 둔다. 나중에 대시보드에서 이 시각을 찾는다.
date +%H:%M:%S

# 컨테이너 안으로 들어가 Java 프로세스를 강제로 죽인다. 정리할 틈도 주지 않는다.
docker compose exec app1 sh -c 'kill -9 $(pgrep -f "java -jar")'

# 곧바로 상태를 확인한다. -a 를 붙여야 죽은 컨테이너도 보인다.
docker compose ps -a app1
```

`kill` 명령 뒤에 Compose가 오류 비슷한 안내를 출력할 수 있다. 앱을 죽이는 순간 `exec`로 들어간 셸도 함께 끊기기 때문이며, 정상이다.

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `docker compose exec app1` | **컨테이너 안에서** 명령을 실행한다. 밖에서 컨테이너를 멈추는 것과 다르다. |
| `pgrep -f "java -jar"` | 컨테이너 안에서 JVM의 PID를 찾는다. `init: true` 덕분에 PID 1이 아니다. |
| `kill -9` | SIGKILL. 프로세스가 반응할 수 없는 신호. 전원이 나간 상황에 가장 가깝다. |

**밖에서 `docker compose kill -s SIGKILL app1`을 쓰면 안 된다.** 이름과 달리 Docker는 이것을 `stop`과 똑같은 **사용자 중지**로 처리해서, 재시작 정책이 동작하지 않는다. 자세한 이유는 이론 7-3에 있다.

**③ 입력·실행 — 터미널 B (죽인 직후, 이어서)**

```bash
# Nginx가 app1 연결에 실패하고 app2로 우회한 흔적을 확인한다.
docker compose logs --tail=20 nginx | grep -E "Connection refused|upstream"

# Prometheus가 본 두 앱의 상태를 확인한다. app1은 0, app2는 1이어야 한다.
curl -s --get http://localhost:9090/api/v1/query \
  --data-urlencode 'query=up{job="petclinic"}'

# 사용자 요청이 여전히 성공하는지, 어느 앱이 처리했는지 확인한다.
for i in $(seq 1 5); do
  curl -s -o /dev/null -w "status=%{http_code}\n" \
    "http://localhost:8080/owners?study=week6-during-$i"
done
grep -Rc "study=week6-during-" logs/tomcat/app1
grep -Rc "study=week6-during-" logs/tomcat/app2

# 20~30초쯤 기다린 뒤 경고가 발생했는지 확인한다.
sleep 30
curl -s -u admin:admin http://localhost:3000/api/alertmanager/grafana/api/v2/alerts \
  | grep -o '"alertname":"[^"]*"'
```

**결과·로그 판정**

| 확인 항목 | 정상 값 | 뜻 |
| --- | --- | --- |
| Nginx 로그 | `Connection refused` + `upstream="IP1:8080, IP2:8080"` | 실패를 스스로 알아채고 app2로 우회했다 |
| `up{app="app1"}` | `"0"` | Prometheus가 app1을 읽지 못한다 |
| `up{app="app2"}` | `"1"` | app2는 정상 |
| `curl` 상태 코드 5건 | 모두 `200` | **사용자는 장애를 겪지 않았다** |
| `grep -Rc` app1 / app2 | `0` / `5` | 살아 있는 앱이 전부 처리했다 |
| 경고 이름 | `PetClinic app DOWN` | 경고가 발생했다 |

**④ 브라우저 (선택)**

`http://localhost:3000` 대시보드를 열면 같은 내용이 그림으로 보인다. app 상태 패널의 `app1`이 빨강(DOWN), 요청 수는 app1이 0·app2가 약 2배, 오류율은 0 근처를 유지한다.

**짧은 설명**

여기서 **네 가지 도구가 각자 무엇을 보고 판단했는지** 구분하는 것이 이번 실습의 목적이다.

| 도구 | 무엇을 보고 판단했나 | 무엇을 했나 |
| --- | --- | --- |
| Nginx | 자기가 보낸 요청이 실패함 | app2로 우회 (5주차와 동일) |
| Docker | 컨테이너 주 프로세스가 죽음 | 컨테이너 다시 실행 |
| Prometheus | 지표를 읽으러 갔다 실패함 | `up=0` 기록 |
| Grafana | Prometheus에 질의한 결과 | 그래프에 표시, 경고 발생 |

**Prometheus가 app1의 장애를 감지했지만, Nginx에게 아무것도 알려 주지 않았다.** Nginx는 자기가 보낸 요청이 실패해서 스스로 알아챘다. 두 경로는 완전히 분리되어 있다.

만약 Nginx가 Prometheus의 판단을 기다렸다면, 수집 간격 5초 + 경고 대기 20초만큼 우회가 늦어졌을 것이다. 그동안 사용자는 오류 화면을 봤을 것이다. **관측은 느려도 되지만 복구는 빨라야 하기 때문에** 두 경로를 나눈 것이다.

---

## 5. 자동 복구 확인

### 5-1. Docker가 다시 실행했는지 확인

> **이 단계에서 검사하는 것:** 사람이 `start`를 입력하지 않았는데도 컨테이너가 되살아났는지, 그리고 그것이 Docker의 동작이었는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 컨테이너 상태와 재시작 횟수를 확인한다. 이름을 직접 써야 죽어 있어도 조회된다.
docker inspect -f 'status={{.State.Status}} restarts={{.RestartCount}} started={{.State.StartedAt}}' \
  spring-petclinic-app1-1

# app1의 기동 로그를 확인한다. 앱이 처음부터 다시 시작한 흔적이 보인다.
docker compose logs --tail 15 app1 | grep -E "Starting|Started|Tomcat"

# 앱이 요청을 받을 준비가 됐는지 확인한다.
docker compose exec nginx wget -qO- http://app1:8080/actuator/health

# Prometheus에서 up이 1로 돌아왔는지 확인한다.
curl -s --get http://localhost:9090/api/v1/query \
  --data-urlencode 'query=up{job="petclinic"}'
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `.RestartCount` | Docker가 이 컨테이너를 **다시 실행한 횟수.** 사람이 `start`한 것은 세지 않는다. |
| `.State.StartedAt` | 마지막으로 시작된 시각. 4-2에서 기록한 `date` 결과와 비교한다. |
| `/actuator/health` | 앱이 요청을 받을 준비가 됐는지 앱 스스로 판단한 결과. |

**결과·로그 판정**

| 확인 항목 | 정상 값 | 뜻 |
| --- | --- | --- |
| `status` | `running` | 컨테이너가 살아 있다 |
| `restarts` | **`1` 이상** | **Docker가 되살렸다는 직접적인 증거** |
| `started` | 4-2에서 죽인 시각의 몇 초 뒤 | 죽자마자 다시 시작됐다 |
| `logs` | `Starting PetClinicApplication` → `Started PetClinicApplication in ...` | 앱이 처음부터 다시 기동했다 |
| `/actuator/health` | `{"status":"UP",...}` | 요청을 받을 준비가 됐다 |
| `up{app="app1"}` | `"1"` | Prometheus가 다시 읽고 있다 |

**자동 복구가 안 될 때**

| 증상 | 원인 | 확인 방법 |
| --- | --- | --- |
| `restarts=0` · `running` · 죽은 적도 없음 | `init: true`가 없어 앱이 애초에 죽지 않았다 | `docker compose exec app1 cat /proc/1/comm` → `java`가 나오면 이것 |
| `restarts=0` · `exited` | 재시작 정책이 붙지 않았거나, 밖에서 `stop`·`kill`로 죽였다 | `docker inspect -f '{{.HostConfig.RestartPolicy.Name}}' spring-petclinic-app1-1` |

두 경우 모두 4-1의 `--force-recreate`부터 다시 한다. 그래도 안 되면 재시작 정책 자체가 동작하는지 앱과 무관한 컨테이너로 갈라서 확인한다.

```bash
# 10초 뒤 스스로 비정상 종료하는 컨테이너. 정책이 정상이면 되살아난다.
docker run -d --name resttest --restart unless-stopped alpine sh -c 'sleep 10; exit 1'
sleep 20
docker inspect -f 'status={{.State.Status}} restarts={{.RestartCount}}' resttest
docker rm -f resttest
```

`restarts=1` 이상이면 Docker는 정상이므로 원인은 위 표에 있다. `restarts=0`이면 Docker 자체의 문제이니 Docker Desktop을 재시작하거나 업데이트한다.

`Started PetClinicApplication in 24.318 seconds` 같은 줄에서 **기동에 걸린 시간**을 기록해 둔다.

**짧은 설명**

복구가 한 번에 끝나지 않는다는 점이 중요하다. 단계마다 시간이 걸린다.

```text
app1의 JVM 강제 종료
  ↓  즉시
컨테이너 종료 (Exited)
  ↓  약 1초        ← Docker가 재시작 결정
컨테이너 다시 시작 (Up)
  ↓  20~60초       ← Spring Boot 기동
PetClinic이 요청을 받을 준비 완료 (health = UP)
  ↓  최대 5초      ← Prometheus 수집 간격
up{app="app1"} = 1
  ↓  최대 10초     ← 5주차의 fail_timeout
Nginx가 app1을 다시 분산 대상으로 사용
```

**`docker compose ps`에 `Up`이 보인다고 서비스가 복구된 것이 아니다.** `Up`은 컨테이너의 프로세스가 시작됐다는 뜻일 뿐이고, Spring Boot가 기동을 마치기까지 20~60초가 더 걸린다. 이 구간에 요청이 가면 실패한다.

Nginx가 `fail_timeout=10s` 뒤에 app1을 다시 시도해 보는 것이 오히려 안전장치가 된다. 아직 기동 중이면 또 실패로 세고 10초를 더 기다린다.

### 5-2. 다시 분산되는지 확인

> **이 단계에서 검사하는 것:** 복구된 app1이 분산 대상으로 돌아와 두 앱이 다시 요청을 나누는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# Nginx의 fail_timeout이 지나기를 기다린다.
sleep 15

# 요청 10건을 보내 다시 나뉘는지 확인한다.
for i in $(seq 1 10); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    "http://localhost:8080/owners?study=week6-after-$i"
done

# 두 앱의 처리 건수를 센다.
grep -Rc "study=week6-after-" logs/tomcat/app1
grep -Rc "study=week6-after-" logs/tomcat/app2

# Prometheus가 본 앱별 처리량도 확인한다.
sleep 15
curl -s --get http://localhost:9090/api/v1/query \
  --data-urlencode 'query=sum by (app) (rate(http_server_requests_seconds_count{job="petclinic", uri!="/actuator/prometheus"}[1m]))'
```

**브라우저 확인**

대시보드에서 **초당 요청 수** 패널을 본다. 시간 범위를 `now-15m`으로 넓히면 전 과정이 한 화면에 보인다.

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| 상태 코드 10건 | 모두 `200` |
| 두 앱 건수 합 | `10`, 양쪽 모두 0이 아님 |
| 대시보드 요청 수 | app1 선이 0에서 올라와 app2와 나란해짐 |
| 대시보드 app 상태 | 둘 다 UP |

app1 건수가 0이면 아직 기동이 안 끝났거나 `fail_timeout` 10초가 지나지 않은 것이다. 잠시 뒤 다시 보낸다. (5주차 6-3과 같은 상황이다)

**짧은 설명**

대시보드의 **초당 요청 수** 패널 하나에 오늘의 이야기가 전부 들어 있다.

```text
     app2 ┃      ╱▔▔▔▔▔▔╲
          ┃─────╱        ╲──────
     app1 ┃─────╲        ╱──────
          ┃      ╲______╱
          └──────────────────────▶ 시간
            정상   장애중    복구후
```

app1이 바닥을 치는 동안 app2가 두 배로 올라갔고, 합계는 거의 그대로다. **합계가 유지됐다는 것이 곧 사용자 요청이 계속 처리됐다는 뜻**이다.

5주차에서는 이걸 `grep -Rc`로 숫자 두 개를 비교해서 알았다. 지금은 시간 축이 있는 그림으로 본다. **몇 시 몇 분에 갈라졌고 몇 초 만에 합쳐졌는지**까지 알 수 있다는 점이 다르다.

### 5-3. 경고가 Normal로 돌아왔는지 확인

> **이 단계에서 검사하는 것:** 경고가 장애 발생과 복구에 맞춰 상태를 바꿨는지 확인한다.

**브라우저 확인 (이 단계는 화면으로 보는 것이 정확하다)**

**Alerting** → **Alert rules** → `PetClinic app DOWN` → 규칙 이름 오른쪽의 **View** → **State history**

상태가 바뀐 기록이 시각과 함께 남아 있다.

**입력·실행 — 터미널 B**

```bash
# 현재 발생 중인 경고를 확인한다. 복구됐다면 비어 있어야 한다.
curl -s -u admin:admin http://localhost:3000/api/alertmanager/grafana/api/v2/alerts \
  | head -c 200
```

**결과·로그 판정**

| 확인 항목 | 정상 값 |
| --- | --- |
| 발생 중인 경고 | `[]` (빈 배열) |
| 브라우저의 규칙 상태 | **Normal** (초록색) |
| State history | `Normal → Pending → Alerting → Normal` 순서로 기록 |

`[]`가 아니라 내용이 들어 있으면 아직 복구가 안 끝났거나, 다른 앱에서 경고가 발생 중인 것이다. `up{job="petclinic"}`을 다시 조회해 확인한다.

**짧은 설명**

경고가 거쳐 온 상태 전이를 정리하면 이렇다.

```text
Normal    app1 정상. up = 1
  ↓  app1 강제 종료, 5초 이내에 up = 0
Pending   조건은 참이 됐지만 for(20초)를 아직 못 채움
  ↓  20초 경과
Alerting  실제 경고 발생. 알림을 보낸다면 이 시점
  ↓  app1 자동 복구, up = 1
Normal    조건이 거짓이 되어 즉시 해제
```

`Pending`이 있는 이유가 이번 단계의 핵심이다. 수집이 한 번 튀었다고 바로 알림이 울리면, **새벽에 의미 없는 알림으로 깨는 일**이 생긴다. `for` 시간은 "이 정도 지속되면 진짜 문제다"라고 정하는 값이다.

실습에서는 20초로 짧게 잡았지만 실제 운영에서는 1~5분을 쓴다. 오늘 본 장애는 자동 복구까지 30~60초가 걸렸으므로, `for: 5m`이었다면 **알림이 울리기 전에 이미 복구가 끝났을 것**이다. 그것이 나쁜 것은 아니다. 사람을 깨우지 않고 스스로 해결된 것이 가장 좋은 결과다.

다만 그 경우에도 **지표는 남는다.** 다음 날 아침에 "새벽 3시 12분에 app1이 죽었다가 41초 만에 복구됐다"를 확인할 수 있다. 이것이 자동 복구와 관측을 함께 만들어야 하는 이유다.

---

## 6. 장애 전·중·후 비교 정리

> **이 단계에서 검사하는 것:** 지금까지 관찰한 것을 하나의 표로 정리해, 이번 주차의 성공 기준을 충족했는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 대시보드의 시간 범위를 넓혀 전 과정을 한 화면에서 본다.
echo "브라우저에서 대시보드 오른쪽 위 시간 범위를 'Last 30 minutes'로 바꾼다"

# 5주차 방식의 최종 대조: 회차별로 두 앱이 각각 몇 건을 처리했는지 센다.
for tag in dash fail during after; do
  echo "--- study=week6-$tag ---"
  echo -n "  app1: "; grep -Rc "study=week6-$tag" logs/tomcat/app1
  echo -n "  app2: "; grep -Rc "study=week6-$tag" logs/tomcat/app2
done
```

**결과·로그 판정 — 비교표**

관찰한 값을 아래 표에 채운다. 빈칸을 채우는 것 자체가 성공 기준의 증거다.

| 확인 항목 | 정상 상태 | app1 장애 중 | app1 복구 후 |
| --- | --- | --- | --- |
| `up{app="app1"}` | `1` | `0` | `1` |
| `up{app="app2"}` | `1` | `1` | `1` |
| app1 초당 요청 수 | 약 10 | `0` | 약 10 |
| app2 초당 요청 수 | 약 10 | **약 20 (2배)** | 약 10 |
| 사용자 응답 코드 | `200` | **`200` 유지** | `200` |
| HTTP 실패율 | 0% | **0%에 가까움** | 0% |
| p95 응답시간 | 장애 전 수준 | **장애 전보다 조금 높음** | 다시 장애 전 수준 |
| Nginx 로그 | 두 IP가 번갈아 | **`upstream`에 IP 두 개** | 다시 번갈아 |
| Grafana 경고 | Normal | **Alerting** | Normal |
| `RestartCount` | `0` | — | **`1` 이상** |

**짧은 설명**

표에서 굵게 표시한 칸이 이번 주차의 결론이다. 각각을 문장으로 읽으면 이렇다.

| 칸 | 문장 |
| --- | --- |
| app2 요청 수 2배 | 살아 있는 앱이 죽은 앱 몫까지 처리했다 |
| 사용자 응답 200 유지 | **사용자는 장애를 겪지 않았다** |
| 실패율 0% | 우회가 요청을 놓치지 않았다 |
| Nginx 로그의 IP 두 개 | 한 요청이 실패 후 재시도되어 성공했다 |
| 경고 Alerting | 사람이 안 보고 있어도 알 수 있었다 |
| RestartCount 1 이상 | 사람이 아니라 Docker가 되살렸다 |

p95는 대시보드의 p95 패널에서 장애 구간 앞뒤를 눈으로 비교한다. 오른쪽 위 시간 범위를 `Last 30 minutes`로 두면 세 구간이 한 화면에 들어온다. p95가 **조금 오르는 것이 정상**이라는 점도 짚어 둔다. 앱 한 대가 두 대 몫을 처리했으니 당연하다. "죽지 않았다"와 "느려지지 않았다"는 다른 이야기다. 5주차까지의 방법으로는 이 차이를 볼 수 없었다.

한 줄로 정리하면 이렇게 말할 수 있게 됐다.

```text
5주차:  "app1을 멈춰도 서비스가 됩니다."

6주차:  "app1이 14시 32분 07초에 비정상 종료됐고,
         그동안 사용자 요청 실패는 0건이었으며,
         p95는 120ms에서 180ms로 올랐고,
         Docker가 41초 만에 자동 복구했습니다."
```

---

## 7. (선택) `stop`과 `kill`의 대조, X-Upstream 헤더, 오류율 경고, k6

시간이 남을 때만 진행한다. 네 항목은 서로 독립적이므로 관심 있는 것만 골라 해도 된다. 다만 **7-2는 7-4보다 먼저** 해야 k6 요약에 앱별 처리 건수가 잡힌다.

### 7-1. `stop`은 왜 자동 재시작이 안 되는가

> **이 단계에서 검사하는 것:** 같은 재시작 정책에서 `stop`과 `kill`의 결과가 다르다는 것을 직접 확인한다.

**입력·실행 — 터미널 B**

```bash
# 현재 재시작 횟수를 기록해 둔다.
docker inspect -f 'restarts={{.RestartCount}}' spring-petclinic-app1-1

# 이번에는 사용자가 정상적으로 중지한다.
docker compose stop app1

# 30초를 기다린 뒤 상태를 확인한다. -a 를 붙여야 죽은 컨테이너도 보인다.
sleep 30 && docker compose ps -a app1

# 재시작 횟수가 그대로인지 확인한다.
docker inspect -f 'status={{.State.Status}} restarts={{.RestartCount}}' spring-petclinic-app1-1

# 사람이 직접 다시 시작한다.
docker compose start app1
sleep 30 && docker compose ps app1
```

여기서는 `$(docker compose ps -q app1)` 대신 **컨테이너 이름을 직접 쓴다.** `ps -q`는 실행 중인 컨테이너만 알려 주므로, 멈춘 컨테이너를 조회하려 하면 빈 값이 되어 `docker inspect`가 오류를 낸다.

**결과·로그 판정**

| 확인 항목 | 4-2의 경우 (안에서 `kill -9`) | `stop`의 경우 |
| --- | --- | --- |
| 30초 뒤 상태 | `running` | **`exited`** |
| `RestartCount` | 1 증가 | **그대로** |
| 복구 방법 | Docker가 자동 | **사람이 `start`** |

**짧은 설명**

같은 `restart: unless-stopped` 정책인데 결과가 다르다. Docker가 **왜 멈췄는지**를 구분하기 때문이다.

```text
docker compose stop app1   → "사용자가 의도적으로 멈춤"   → 되살리지 않음
docker compose kill app1   → "사용자가 의도적으로 멈춤"   → 되살리지 않음
컨테이너 안에서 앱이 죽음  → "사고"                      → 되살림
```

가운데 줄이 함정이다. `kill`은 이름 때문에 사고처럼 보이지만, **밖에서 Docker에게 보낸 요청**이라는 점에서 `stop`과 똑같이 취급된다. 기준은 신호의 종류가 아니라 **누가 죽였는가**다.

`unless-stopped`의 "unless(~하지 않는 한)"가 가리키는 것이 바로 이 구분이다. 사용자가 멈춘 것까지 되살리면 **컨테이너를 멈출 방법이 없어진다.**

5주차 실습에서 `stop`을 썼기 때문에 자동 재시작이 일어나지 않았던 것이기도 하다. 두 실습은 같은 앱을 다른 방식으로 죽여 본 것이다.

### 7-2. 응답 헤더로 앱을 구분하기

> **이 단계에서 검사하는 것:** 어느 앱이 응답했는지 응답 헤더 한 줄로 바로 알 수 있게 만든다.

지금까지는 어느 앱이 처리했는지 알려면 `grep -Rc`로 Tomcat 로그를 세거나 Nginx 로그를 읽어야 했다. 응답 헤더로 알려 주면 요청 하나하나에서 곧바로 확인된다. 7-4의 k6 요약에서 앱별 처리 건수를 세는 데도 이 헤더가 쓰인다.

**입력·수정 — 터미널 B**

```bash
# Nginx 설정을 연다.
nano nginx/conf.d/default.conf
```

일반 `location /` 안, `proxy_pass` 아래에 추가한다.

```nginx
# 어느 app이 응답했는지 헤더로 알려 준다. (실습 설명용)
add_header X-Upstream $upstream_addr always;
```

**입력·실행 — 터미널 B**

```bash
# 앱이 모두 실행 중인 상태에서 문법을 검사한다. (5주차 규칙)
docker compose exec nginx nginx -t

# 검사가 통과한 설정을 반영한다.
docker compose exec nginx nginx -s reload

# 헤더가 실제로 붙는지 확인한다.
curl -sI "http://localhost:8080/owners?study=week6-header" | grep -i "x-upstream"

# 여러 번 보내면 두 앱의 주소가 번갈아 나오는지 확인한다.
for i in $(seq 1 6); do
  curl -sI "http://localhost:8080/owners?study=week6-header" | grep -i "x-upstream"
done
```

**결과·로그 판정**

- `curl -sI` 결과에 `X-Upstream: 172.x.x.x:8080` 줄이 있다.
- 6번 반복하면 **서로 다른 IP 두 개**가 번갈아 나온다. 5주차의 라운드 로빈이 헤더로 보이는 것이다.

**짧은 설명**

`$upstream_addr`는 5주차의 로그 형식에서 이미 쓰던 변수다. 같은 값을 로그가 아니라 **응답 헤더로도** 내보내는 것뿐이다.

다만 이 헤더는 **내부 컨테이너 IP를 밖으로 노출한다.** 실습에서는 설명을 위해 쓰지만, 실제 서비스에서는 넣지 않는다. 공격자에게 내부 구조를 알려 주는 정보가 되기 때문이다.

`always`를 붙인 이유는, 이 옵션이 없으면 200 같은 성공 응답에만 헤더가 붙고 오류 응답에는 붙지 않기 때문이다. 장애 상황을 관찰하려면 오류에도 붙어야 한다.

### 7-3. 오류율 경고 확인

> **이 단계에서 검사하는 것:** 두 번째 경고 규칙이 동작하는 상황을 만들어 본다.

`alert-rules.yml`에는 5xx 오류율 경고도 들어 있다. 지금까지는 Nginx 우회 덕분에 오류율이 0이라 한 번도 발생하지 않았다.

**입력·실행 — 터미널 C**

```bash
# 부하를 흘린다. 4-2와 같은 방식이다. 끝낼 때는 Ctrl+C.
while true; do
  curl -s -o /dev/null -w "%{http_code} " "http://localhost:8080/owners?study=week6-allfail"
  sleep 0.2
done
```

**입력·실행 — 터미널 B**

```bash
# 두 앱을 모두 죽인다. 우회할 곳이 없어진다.
docker compose exec app1 sh -c 'kill -9 $(pgrep -f "java -jar")'
docker compose exec app2 sh -c 'kill -9 $(pgrep -f "java -jar")'

# 사용자 응답을 확인한다.
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8080/owners?study=week6-502"

# 30초쯤 뒤 경고를 확인한다.
sleep 40
curl -s -u admin:admin http://localhost:3000/api/alertmanager/grafana/api/v2/alerts \
  | grep -o '"alertname":"[^"]*"'
```

**결과·로그 판정**

| 확인 항목 | 값 | 뜻 |
| --- | --- | --- |
| 사용자 응답 | `502` | 5주차 6-1과 같다. 전달할 앱이 없다 |
| 발생 경고 | `PetClinic app DOWN` **두 건** | app1, app2 각각 |
| 5xx 오류율 경고 | **발생하지 않을 수 있다** | 아래 설명 참고 |

**짧은 설명**

여기서 관측의 한계가 드러난다. 두 앱이 모두 죽으면 **502를 만든 것은 Nginx**이고, 앱은 아무것도 기록하지 못한다.

```text
502를 만든 주체: Nginx
5xx 지표를 만드는 주체: 앱의 Micrometer
→ 앱이 죽었으므로 5xx 지표 자체가 만들어지지 않는다
```

그래서 `http_server_requests_seconds_count{status=~"5.."}`는 늘어나지 않고, 오류율 경고도 울리지 않는다. **사용자는 502를 받고 있는데 앱 지표만 보면 아무 일도 없어 보인다.**

이것이 앱 지표만으로는 부족한 이유이며, 실제 운영에서 Nginx 자체의 지표(`nginx-prometheus-exporter` 등)나 외부에서 서비스를 직접 찔러 보는 감시(blackbox 방식)를 함께 쓰는 이유다.

`up` 지표가 이 빈틈을 메워 준다. app1·app2가 모두 `up=0`이면 오류율을 보지 않아도 심각한 상황임을 알 수 있다. **여러 각도의 지표가 필요한 이유**가 여기 있다.

**복구**

```bash
# 두 앱은 kill로 죽였으므로 Docker가 자동으로 되살린다. 기다린다.
sleep 45 && docker compose ps

# 복구를 확인한다.
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8080/owners?study=week6-recovered"
```

---

### 7-4. k6로 같은 조건의 시험을 반복하기

> **이 단계에서 검사하는 것:** 손으로 만든 `while` 루프 대신, **초당 요청 수를 일정하게 유지하는 전용 도구**로 부하를 주고 결과를 숫자로 받아 본다.

4-2에서는 `while true` + `curl`로 부하를 만들었다. 간단하지만 한계가 있다. 서버가 느려지면 `curl` 한 건이 오래 걸려 **초당 요청 수가 함께 줄어든다.** 그러면 "같은 부하에서 어떻게 달랐는가"를 말할 수 없다. k6의 `constant-arrival-rate`는 응답이 느려져도 초당 건수를 유지한다.

**입력·수정 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 부하 스크립트를 만든다.
nano k6/load.js
```

```javascript
import http from 'k6/http';
import { check } from 'k6';
import { Counter } from 'k6/metrics';

// 컨테이너 안에서 실행되므로 localhost 가 아니라 서비스명으로 접속한다.
const BASE_URL = __ENV.BASE_URL || 'http://nginx:80';
const DURATION = __ENV.DURATION || '2m';
const TAG = __ENV.TAG || 'base';
const RATE = Number(__ENV.RATE || 20);

// Nginx 가 붙여 주는 X-Upstream 헤더로 앱별 처리 건수를 센다. (7-2에서 추가)
const app1Hits = new Counter('app1_hits');
const app2Hits = new Counter('app2_hits');

export const options = {
  scenarios: {
    steady: {
      executor: 'constant-arrival-rate',
      rate: RATE,
      timeUnit: '1s',
      duration: DURATION,
      // 응답이 느려져도 초당 요청 수를 유지할 수 있도록 여유를 둔다.
      preAllocatedVUs: Math.max(20, RATE * 2),
      maxVUs: Math.max(100, RATE * 10),
      gracefulStop: '10s',
    },
  },
  // 장애를 일부러 만드는 실습이므로 실패해도 시험을 중단하지 않는다.
  thresholds: {
    http_req_failed: [{ threshold: 'rate<1.01', abortOnFail: false }],
  },
  // p99 는 k6 기본 집계에 없다. 요약표에 찍으려면 여기서 요청해야 한다.
  summaryTrendStats: ['avg', 'min', 'med', 'max', 'p(50)', 'p(95)', 'p(99)'],
};

export default function () {
  // 5주차와 같은 방식으로 로그에서 찾을 수 있도록 표식을 붙인다.
  const res = http.get(`${BASE_URL}/owners?study=week6-${TAG}`, {
    tags: { name: '/owners' },
  });

  check(res, { 'status is 200': (r) => r.status === 200 });

  // 어느 앱이 처리했는지 응답 헤더로 센다. 7-2를 하지 않았다면 헤더가 없어 0으로 남는다.
  const upstream = res.headers['X-Upstream'] || res.headers['x-upstream'];
  if (upstream && upstream.endsWith(':8080')) {
    if (!globalThis.__firstUpstream) globalThis.__firstUpstream = upstream;
    if (upstream === globalThis.__firstUpstream) app1Hits.add(1, { upstream });
    else app2Hits.add(1, { upstream });
  }
}

// k6 기본 요약 대신 6주차 기록지에 옮겨 적을 표를 직접 만든다.
export function handleSummary(data) {
  const m = data.metrics;
  const get = (name, field, fallback = 0) => {
    if (!m[name] || m[name].values[field] === undefined) return fallback;
    return m[name].values[field];
  };

  const ms = (v) => `${v.toFixed(1)} ms`;
  const row = (label, value) => `  ${label.padEnd(24)} ${value}`;

  const text = [
    '',
    '================ 6주차 기록용 요약 ================',
    row('회차 표식(TAG)', `week6-${TAG}`),
    row('시험 시간', DURATION),
    row('목표 초당 요청 수', `${RATE} req/s`),
    '  ' + '-'.repeat(46),
    row('전체 요청 수', `${get('http_reqs', 'count')}`),
    row('초당 요청 수', `${get('http_reqs', 'rate').toFixed(2)} req/s`),
    row('HTTP 실패율', `${(get('http_req_failed', 'rate') * 100).toFixed(2)} %`),
    '  ' + '-'.repeat(46),
    row('응답시간 평균', ms(get('http_req_duration', 'avg'))),
    row('응답시간 p50', ms(get('http_req_duration', 'med'))),
    row('응답시간 p95', ms(get('http_req_duration', 'p(95)'))),
    row('응답시간 p99', ms(get('http_req_duration', 'p(99)'))),
    row('응답시간 최대', ms(get('http_req_duration', 'max'))),
    '  ' + '-'.repeat(46),
    row('app1 처리 건수', `${get('app1_hits', 'count')}`),
    row('app2 처리 건수', `${get('app2_hits', 'count')}`),
    '===================================================',
    '',
    '  * app 처리 건수가 0이면 Nginx에 X-Upstream 헤더가 아직 없는 것이다. (7-2)',
    '',
  ].join('\n');

  return {
    stdout: text,
    // 회차별로 파일이 남는다. ls -l k6/summary-*.json 로 확인한다.
    [`/scripts/summary-${TAG}.json`]: JSON.stringify(data, null, 2),
  };
}
```

> **대안 —** 입력이 길다면 아래 한 줄로 대신 받아도 된다.
>
> ```bash
> curl -fsSL -o k6/load.js \
>   https://raw.githubusercontent.com/likelion-server-study/WEEK6/main/files/k6/load.js
> ```
>
> 여기서도 `429`가 나면 3-1의 대안 1처럼 저장소를 받아서 복사한다.
>
> ```bash
> git clone --depth 1 https://github.com/likelion-server-study/WEEK6.git /tmp/week6
> cp /tmp/week6/files/k6/load.js k6/
> ```

```bash
# 스크립트가 제자리에 있는지 확인한다.
ls -l k6/load.js

# k6 서비스를 추가하기 위해 Compose 파일을 연다.
nano docker-compose.yml
```

`grafana` 서비스 다음에 추가한다.

```yaml
  k6:
    image: grafana/k6:latest
    profiles: ["load"]
    volumes:
      - ./k6:/scripts
    environment:
      BASE_URL: http://nginx:80
    networks:
      - proxy-net
```

> **주의 1:** `./k6:/scripts`에 `:ro`를 붙이지 않는다. k6가 결과 요약 JSON을 이 폴더에 쓴다.
>
> **주의 2:** `container_name`을 지정하지 않는다. `docker compose run`은 실행할 때마다 새 컨테이너를 만들기 때문에 이름이 충돌한다.

**입력·실행 — 터미널 B**

```bash
# 문법을 확인한다.
docker compose config >/dev/null && echo "Compose 문법 정상"

# k6가 profiles 때문에 평소에는 뜨지 않는지 확인한다.
docker compose config --services
docker compose ps

# 정상 상태에서 초당 20건을 2분 동안 일정하게 보낸다.
docker compose --profile load run --rm -e DURATION=2m -e TAG=base k6 run /scripts/load.js
```

**브라우저 확인**

시험이 도는 2분 동안 `http://localhost:3000` 대시보드를 함께 본다.

**입력·실행 — 터미널 B (시험이 끝난 뒤)**

```bash
# 5주차 방식으로 앱별 처리 건수를 센다. k6 요약과 맞는지 대조한다.
grep -Rc "study=week6-base" logs/tomcat/app1
grep -Rc "study=week6-base" logs/tomcat/app2
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `constant-arrival-rate` | 응답이 느려져도 **초당 요청 수를 유지한다.** `while` 루프와 가장 크게 다른 점이다. |
| `profiles: ["load"]` | 이 서비스는 `docker compose up`으로 **뜨지 않는다.** 부하 도구가 계속 돌면 곤란하기 때문이다. |
| `docker compose run --rm` | 서비스를 **한 번만** 실행하고 끝나면 컨테이너를 지운다. |
| `--profile load` | profiles가 붙은 서비스를 실행 대상에 포함시킨다. |
| `-e DURATION=2m` | 스크립트가 읽는 환경 변수를 그때그때 바꾼다. |
| `BASE_URL: http://nginx:80` | 컨테이너 안에서 실행되므로 `localhost`가 아니라 **서비스명**으로 접속한다. |

**결과·로그 판정**

- `docker compose config --services`에는 `k6`가 보이지만 `docker compose ps`에는 안 보인다.
- 시험이 끝나고 `================ 6주차 기록용 요약 ================` 표가 출력된다.

| 항목 | 정상 상태의 대략적인 값 |
| --- | --- |
| 전체 요청 수 | 약 `2400` (20 req/s × 120초) |
| 초당 요청 수 | 약 `20` |
| p95 응답시간 | 수십~수백 ms |
| HTTP 실패율 | `0.00 %` |

`grep -Rc` 결과는 두 앱의 합이 전체 요청 수와 비슷하고, **양쪽이 대략 반반**이어야 한다.

실패율이 100%면 `BASE_URL`이 잘못된 것이다. 컨테이너 안에서는 `http://nginx:80`이어야 한다.

**더 해 볼 것**

이 상태에서 4단계를 다시 해 보면 6단계의 비교표를 **숫자로** 채울 수 있다. 터미널 C에서 `-e DURATION=5m -e TAG=fail`로 k6를 띄워 두고, 터미널 B에서 `docker compose exec app1 sh -c 'kill -9 $(pgrep -f "java -jar")'`을 실행한다. 시험이 끝나면 요약표의 p95와 실패율이 `TAG=base` 때와 얼마나 다른지 비교한다.

**짧은 설명**

`profiles`가 없으면 `docker compose up -d`를 할 때마다 부하 도구가 함께 실행돼 계속 요청을 보낸다. 시험 도구는 **필요할 때만** 돌아야 한다.

`BASE_URL`을 `localhost:8080`으로 쓰면 안 되는 이유는 5주차의 `proxy_pass`와 같은 이유다. 컨테이너 안에서 `localhost`는 **그 컨테이너 자신**을 가리킨다. k6 컨테이너 안에는 웹 서버가 없으므로 연결이 거부된다.

k6가 센 요청 수와 `grep -Rc`가 센 건수가 비슷하게 나오는 것도 확인해 둔다. **새 도구의 숫자를 이미 검증된 방법으로 맞춰 본 것**이다.

---

## 종료

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 터미널 A와 C에서 Ctrl+C로 로그 보기와 부하를 먼저 끝낸다.

# 실습 컨테이너와 네트워크를 종료하되 데이터 볼륨은 보존한다.
docker compose down

# 볼륨이 남아 있는지 확인한다.
docker volume ls | grep -E "petclinic|prometheus|grafana"
```

`docker compose down -v`는 실행하지 않는다. PostgreSQL 데이터뿐 아니라 Prometheus가 수집한 지표와 Grafana 설정까지 모두 사라진다.

### 오늘의 성공 기준

- [ ] PetClinic에 `micrometer-registry-prometheus`를 추가하고 이미지를 다시 만들었다. (1-2, 1-3)
- [ ] `app1`·`app2` 모두 `/actuator/prometheus`에서 지표를 내놓는다. (1-4)
- [ ] 응답시간 히스토그램 버킷(`..._bucket`)이 지표에 포함돼 있다. (1-4)
- [ ] Prometheus가 Nginx를 거치지 않고 두 앱을 **각각** 수집한다. (2-3)
- [ ] `up{job="petclinic"}` 조회 결과가 `app1`, `app2` 두 개로 나온다. (2-3)
- [ ] Grafana 대시보드에서 요청량·응답시간·JVM·DB 지표를 볼 수 있다. (3-3)
- [ ] p95 응답시간 패널에 값이 그려진다. (3-3)
- [ ] 경고 규칙이 Normal 상태로 등록돼 있다. (3-4)
- [ ] `app1`을 강제 종료하자 Prometheus에서 `up`이 `0`이 됐다. (4-2)
- [ ] 장애 중에도 Nginx가 app2로 우회해 사용자 응답이 `200`을 유지했다. (4-2)
- [ ] Nginx access log에서 `upstream`에 주소가 **두 개** 기록된 줄을 찾았다. (4-2)
- [ ] Grafana 경고가 Pending을 거쳐 Alerting으로 바뀌었다. (4-2)
- [ ] Docker가 비정상 종료된 `app1`을 다시 실행했다. (`RestartCount` 증가) (5-1)
- [ ] 복구 후 두 앱이 다시 요청을 나누어 처리한다. (5-2)
- [ ] 경고가 Normal로 돌아왔다. (5-3)
- [ ] 장애 전·중·후의 응답시간과 실패율을 비교해 기록했다. (6)
- [ ] Prometheus가 장애를 감지해도 Nginx의 요청 분배를 제어하지 않는다는 것을 설명할 수 있다.

## 부록. 자주 만나는 오류

| 증상 | 원인 | 해결 |
| --- | --- | --- |
| `/actuator/prometheus`가 404 | `micrometer-registry-prometheus` 누락 | `grep -n micrometer build.gradle` 확인 후 다시 빌드 (1-2) |
| 의존성을 넣었는데 그대로 404 | 이미지에 옛 JAR가 들어 있다 | `./gradlew bootJar` → `docker build` → `up -d --force-recreate` (1-3) |
| `/actuator/prometheus`는 되는데 지표가 거의 없다 | 요청이 한 건도 없었다 | `/owners`를 몇 번 호출한 뒤 다시 확인 (1-4) |
| Prometheus targets가 DOWN | 대상을 `localhost:8080`으로 썼다 | Compose 안에서는 `app1:8080`, `app2:8080` (2-1) |
| Prometheus targets가 DOWN | `proxy-net`에 안 붙였다 | `docker inspect week6-prometheus \| grep -A5 Networks` (2-2) |
| `error loading config` | `prometheus.yml` 들여쓰기 문제 | YAML은 탭이 아니라 **공백**만 쓴다 (2-1) |
| Compose가 "undefined volume" 오류 | `volumes:` 블록에 선언을 빠뜨렸다 | 파일 맨 아래에 `prometheus-data:` 추가 (2-2) |
| Grafana 대시보드가 아예 없다 | 지시서의 `path`와 볼륨 경로가 다르다 | 둘 다 `/var/lib/grafana/dashboards`인지 확인 (3-1, 3-2) |
| 모든 패널이 "datasource not found" | 데이터소스 `uid` 불일치 | `uid: prometheus`가 있는지 확인 (3-1) |
| **p95 패널만 비어 있다** | 히스토그램 버킷이 없다 | `percentiles-histogram=true` 확인 후 **다시 빌드** (1-2, 1-3) |
| 경고 규칙이 등록되지 않았다 | `alert-rules.yml` 형식 오류 | `docker compose logs grafana \| grep -i alert` (3-2) |
| `curl: (56) ... error: 429` | GitHub이 요청을 일시적으로 거절했다 | 파일·주소 문제가 아니다. `git clone`으로 받아서 복사한다 (3-1의 대안) |
| k6 실패율이 100% | `BASE_URL`이 잘못됐다 | 컨테이너 안에서는 `http://nginx:80` (7-4) |
| k6가 요약 파일을 못 쓴다 | `./k6:/scripts`에 `:ro`를 붙였다 | `:ro`를 지운다 (7-4) |
| `kill`했는데 재시작이 안 된다 | 재시작 정책이 컨테이너에 없다 | `docker inspect -f '{{.HostConfig.RestartPolicy.Name}}'` 확인 후 `--force-recreate` (4-1) |
| 컨테이너는 `Up`인데 계속 502 | Spring Boot 기동이 아직 안 끝났다 | `docker compose logs -f app1`에서 `Started` 확인 (5-1) |
| 복구 후에도 app1에 요청이 안 간다 | `fail_timeout` 10초가 안 지났다 | 15초쯤 기다린 뒤 다시 요청 (5-2) |
| 포트 충돌 (3000/9090) | 다른 프로그램이 쓰고 있다 | Compose `ports`의 **왼쪽 숫자만** 변경 (01-환경세팅 4절) |
| 컨테이너가 자꾸 죽는다 | Docker 메모리 부족 | Docker Desktop의 Memory를 4GB 이상으로 (01-환경세팅 1절) |
| 지표는 나오는데 그래프가 계단 모양 | 요청이 드문드문 들어간다 | 정상. 부하를 계속 흘리면 매끄러워진다 (4-2) |
